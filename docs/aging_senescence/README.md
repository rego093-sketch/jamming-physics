# docs/ — CANONICAL HTML (built only after research gates are green)

Per VP-SPEC v1.8 the canonical artifact is per-title HTML here (one <slug>/index.html per section, answer-first, JSON-LD, English body). DOI: TBD (filled on publication). Empty until the writing phase unlocks.
