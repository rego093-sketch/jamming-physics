# HANDOFF — read this first (new session, cold start)

> **STOP. If you are a new session opening this archive, read this file top to bottom
> before doing anything else.** This package is a self-contained, long-term research
> program. Everything you need to continue the research is inside this single zip —
> you do **not** need any other file, and you do **not** need the two source
> whitepapers it inherited from (their inherited content is captured in
> `INHERITANCE_MANIFEST.md`).

**Language (binding, going forward):** all new work in this program is written in
**English only**. Do not reintroduce mixed-language prose.

**Physical realization is deferred.** This is a **theory track**. Do not block on
"which physical medium." Build and stress-test the theory; hardware is a later task.

**Firewall (binding, every layer):** this program builds **function** (capability)
only. `consciousness_claim = 0`, `hard_problem_open = 1`. The hard problem (felt
quality / qualia) is an **explicit blank** that is never erased. "At least
human-level intelligence" here is a **functional** claim, not a consciousness claim.

**Provenance / citation.** Author **Young Jae Lee** (ORCID `0009-0002-7535-8245`),
program **jamming-physics.org**, license **CC BY 4.0**. **Concept DOI
`10.5281/zenodo.20783570`** (all versions). Metadata lives in `CITATION.cff` (registered
in the §4 manifest); keep it in any future export. Do not remove or alter the DOI.

---

## 1. What this package is (one paragraph)

A wave-substrate computer: information is carried by the **phase** of coupled
oscillators, not by bit-amplitudes. The brain reaches general intelligence on a wave
substrate with specific properties (phase coding, resonance matching, one-shot
learning, metastable dynamics, noise immunity, "sum = information" superposition).
This program (a) **proved** those properties on the substrate (Layer 0, done), and
(b) lays a **blueprint** (Layers 1–9) that tests, layer by layer and falsifiably,
whether those properties are **sufficient** for human-level functional intelligence.
The substrate is inherited from two frozen whitepapers — the brain engine
`vp_frontal v2` (ephaptic/phase principles) and the jamming physics `vp_physics
v0.11.0` (wave-in-medium, clock-free, near-field). See `INHERITANCE_MANIFEST.md`.

---

## 2. BINDING — learn the existing program BEFORE adding anything

You may not write a single new line of research code until you have **internalized**
the four inherited things below. This is not optional. A session that adds work
without first learning these will drift from the program and is a regression.

**Read, in this order, and confirm you have absorbed each:**

1. **`INHERITANCE_MANIFEST.md` — the inheritance discipline.** What was taken from
   each source whitepaper (brain B1–B5, physics P1–P3), the **firewall**, and the
   **no-tuning** rule. The inherited numbers (e.g. the mind engine anchor
   `R = 0.38961455156044245`) are **principles/provenance, never fitting targets**.
2. **`CUMULATIVE_LOG.md` — what is already proven.** The substrate L0 (sessions S1,
   S2) and every result + its grade `[V]`/`[L]`/`[O]` + every recorded honest
   negative. Do not re-derive or re-claim what is already settled; do not contradict
   a recorded negative.
3. **`BLUEPRINT_toward_ultimate_computer.md` — where the program is going.** The
   nine layers, the dependency graph, each layer's milestone + the stress test
   **designed to break it**, the end condition, and the **Stress Principle** (every
   hypothesis is stress-tested to destruction; a collapse is recorded and the line
   restarts with the collapse built in — collapses are never papered over).
4. **§4 below — the regression rule.** The file manifest is fixed and additive-only;
   prior session records are byte-stable; the frozen substrate L0 is never modified.

**Self-check before proceeding (answer all "yes"):**
- Can I state the four inherited brain principles and the three inherited physics
  principles, and the firewall, without opening the source whitepapers? *(They are
  all in `INHERITANCE_MANIFEST.md` — the package is self-contained.)*
- Do I know the current grade of every layer and every standing `[O]` risk?
- Do I know the **next chunk** and the stress test that will try to break it?
- Do I understand that I deliver **exactly one zip**, additive, with nothing dropped?

If any answer is "no", re-read. Only then continue.

---

## 3. Autonomous research protocol (toward the blueprint)

You are expected to **proceed autonomously toward the blueprint** — do not wait for
permission to do the obvious next step. Autonomy operates strictly **under the
rules** (§5).

**The loop each session:**
1. **Pick the next chunk** from `BLUEPRINT … §12` (the dependency graph names it).
   The current next chunk is recorded in `START_HERE_wave_computer.md §4` and
   `BLUEPRINT … §12`. *(As of S10: **THE PROGRAM IS COMPLETE — the blueprint CLOSES.** S10 built
   **L9 — functional general intelligence (THE END CONDITION)** on `repro/wave_agi_core.py` (digest
   `20b5f2c2…`): the integrated L0–L8 machine assessed against the **capability ladder** =
   **6/7 rungs `[V]`** — A1 compositional/multi-item (capacity K\* **emerges** 6/10 by N, toward θ–γ
   ~7; no-tag control recovers single) [V], A2 one-shot (≈1.0 ≫ chance) [V], **A3 real-time adaptation
   [O]** the lone shortfall (a single additive store cannot **over-write** a switched rule; recovery
   0.78/0.58 < band) with the **proven L5 dual store** (A7) as the named mitigation, A4 noise-immersed
   robustness (D4 **lifts fidelity**, off-lattice `corrupt_phase`; accuracy over-determined, stated)
   [V], A5 scale CAM (hierarchy holds 1.0 past flat ceiling T=16, O(1)-in-T) [V], A6 cross-domain
   transfer (filler-independent, ceiling J\*=3) [V], A7 open-ended acquisition (dual ~1.0 vs single
   decaying) [V]. The sufficiency hypothesis is **settled**; closing is **not** a claim of human
   intelligence. Two methodological corrections documented transparently (A4 lattice-freeze fixed by
   adopting the inherited noise model; A3 baseline cleaned, outcome unchanged). Firewall held
   throughout (`consciousness_claim=0`, `hard_problem_open=1`, `new_tuned_constants=0`).*
   **There is no further layer.** *Any future session is **post-program** and OPTIONAL:* (a)
   **compression** — an axiom-independence audit of the inherited invariants; (b) **hardening** —
   convergent-evidence grading and wider sweeps (e.g. widen the A1 K×N sweep, trimmed on a single-core
   box; re-probe the A3 single-store `[O]` with the dual store wired in to show the `[O]→[V]` closure
   inline). **As of S11 (v0.11): the A3 inline closure was executed** — `repro/wave_adapt_closure_core.py`
   (digest `ad93057afac2054e…`) re-probes A3 with the dual store wired in on the adaptation task itself
   (single store = exactly the dual's SLOW component alone; the only change is the added fast R3/C2
   episodic field). Single fails the full switch (0.583 < band, reproducing 0.78/0.58); the dual store
   recovers to 1.000 at every shift fraction → A3 closes `[O]→[V]`, sign-stable across size; the raw
   unnormalized dual sum also beats single, so the normalization is scale-equalizing only. **Ladder
   now 7/7 `[V]` with the dual store wired in; the S10 6/7 single-store `[O]` record STANDS** as the
   minimal machine's honest limit (paired, not overwritten). **As of S12 (v0.12): the compression
   audit (a) was also executed** — `repro/wave_axiom_audit_core.py` (digest `7f59ced681bd…`)
   compresses the 8 inherited invariants onto 5 operational L0 axioms (AX1 data field, AX2 symmetry,
   AX3 nonlinearity, AX4 settling, AX5 band; B5 theta-gamma deferred as a higher-layer realization),
   then nulls each axiom with the others intact under one non-circular probe, paired & sign-stable
   across 6 seeds, gated by a passing intact control — **all five collapse → all load-bearing**,
   `redundant_axioms=[]`, `axiom_set_irreducible=True` (minimal core, no further compression); the L0
   substrate is never edited. **Both named continuations (a)+(b) are now executed.** What remains
   optional and post-program: only further hardening such as widening the A1 K×N sweep. No new
   layer, no tuning. The discipline below (frozen substrate, sweeps, non-circular
   read-outs, honest grading, one additive zip) **still binds** if a post-program session is run.*
2. **Build it on the frozen L0 substrate.** Reuse `repro/wave_compute_core.py`
   primitives exactly (non-circular). New code goes in `repro/` as a new module;
   never edit the frozen substrate or prior modules.
3. **State the milestone and the break-condition up front**, then run a **sweep**
   (seeds × the relevant axes). A claim is believed only if **sign-stable** across
   the sweep. Make the read-outs **non-circular** (a read-out must not be a direct
   function of the thing perturbed).
4. **Record the result honestly.** Grade `[V]`/`[L]`/`[O]`. If the stress test
   **breaks** the hypothesis, record the break in the results, set the layer's
   status accordingly, and **restart that line next session with the break applied**.
   Do not hide or soften a collapse.
5. **Pin a digest** (`expected_digest_*.json`) and emit a figure. Determinism is
   required (fixed seeds; re-runs reproduce bit-for-bit).
6. **Update the docs**: append to `CUMULATIVE_LOG.md`, write a new
   `SESSION_v0_k_*.md`, update `START_HERE` (status + session ledger) and the
   `BLUEPRINT` (layer status + the next chunk).
7. **Repackage as ONE zip** (§4) and stop. If a session has no budget for new
   research, do **only** the packaging/bookkeeping step and stop.

**Scope discipline:** one session does **one chunk** (or part of one). Bounded load.
Do not attempt the whole blueprint in one session.

---

## 4. Regression prevention (no file may be dropped)

**The package is additive-only.** Every file listed in `FILE_MANIFEST` (below) must
be present in every future zip. Prior `SESSION_v0_k_*.md` records and the frozen
substrate are **byte-stable** — never rewritten. New sessions **add** entries; they
never remove or silently mutate.

**Canonical file manifest (must all be present):**
```
HANDOFF.md                              ← this file (cold-start entry)
START_HERE_wave_computer.md             ← master living doc (status, ledger, pointers)
INHERITANCE_MANIFEST.md                 ← self-contained inheritance (no external file needed)
BLUEPRINT_toward_ultimate_computer.md   ← long-term blueprint (L0–L9)
CUMULATIVE_LOG.md                       ← cumulative work history (append-only)
SESSION_v0_1_design_study.md            ← S1 record (byte-stable)
SESSION_v0_2_resonance_and_learning.md  ← S2 record (byte-stable)
SESSION_v0_3_structure_and_time.md      ← S3 record (byte-stable)
SESSION_v0_4_hierarchy_and_abstraction.md ← S4 record (byte-stable)
SESSION_v0_5_inference_and_resonance.md ← S5 record (byte-stable)
SESSION_v0_6_dual_learning_systems.md   ← S6 record (byte-stable)
SESSION_v0_7_self_supervised_world_model.md ← S7 record (byte-stable)
SESSION_v0_8_embodiment_and_control.md  ← S8 record (byte-stable)
SESSION_v0_9_global_integration_and_access.md ← S9 record (byte-stable)
SESSION_v0_10_functional_general_intelligence.md ← S10 record (L9 END CONDITION; byte-stable)
SESSION_v0_11_adaptation_closure.md     ← S11 record (POST-PROGRAM HARDENING: A3 [O]→[V] closed inline; byte-stable going forward)
SESSION_v0_12_axiom_independence_audit.md ← S12 record (POST-PROGRAM COMPRESSION: axiom-independence audit, 8→5 irreducible; byte-stable going forward)
CITATION.cff                            ← citation metadata (concept DOI 10.5281/zenodo.20783570)
repro/wave_compute_core.py              ← FROZEN substrate L0 (READ-ONLY; never edit)
repro/wave_compute_results.json         ← S1 results (digest e9fdd3bc…)
repro/wave_compute_atlas.png            ← S1 figure
repro/make_figure.py                    ← S1 figure generator
repro/expected_digest.json              ← S1 pinned digest
repro/wave_resonance_core.py            ← S2 module (reuses L0; READ-ONLY going forward)
repro/wave_resonance_results.json       ← S2 results (digest c69917…)
repro/wave_resonance_atlas.png          ← S2 figure
repro/make_figure_v0_2.py               ← S2 figure generator
repro/expected_digest_v0_2.json         ← S2 pinned digest
repro/wave_structure_core.py            ← S3 module (reuses L0+L1; READ-ONLY going forward)
repro/wave_structure_results.json       ← S3 results (digest 69890fec…)
repro/wave_structure_atlas.png          ← S3 figure
repro/make_figure_v0_3.py               ← S3 figure generator
repro/expected_digest_v0_3.json         ← S3 pinned digest
repro/wave_hierarchy_core.py            ← S4 module (reuses L0+L1+L2; READ-ONLY going forward)
repro/wave_hierarchy_results.json       ← S4 results (digest ea4c6723…)
repro/wave_hierarchy_atlas.png          ← S4 figure
repro/make_figure_v0_4.py               ← S4 figure generator
repro/expected_digest_v0_4.json         ← S4 pinned digest
repro/wave_inference_core.py            ← S5 module (reuses L0+L1+L3; READ-ONLY going forward)
repro/wave_inference_results.json       ← S5 results (digest 2d3057bb…)
repro/wave_inference_atlas.png          ← S5 figure
repro/make_figure_v0_5.py               ← S5 figure generator
repro/expected_digest_v0_5.json         ← S5 pinned digest
repro/wave_consolidation_core.py        ← S6 module (reuses L0+L3; READ-ONLY going forward)
repro/wave_consolidation_results.json   ← S6 results (digest e8a623…)
repro/wave_consolidation_atlas.png      ← S6 figure
repro/make_figure_v0_6.py               ← S6 figure generator
repro/expected_digest_v0_6.json         ← S6 pinned digest
repro/wave_world_model_core.py          ← S7 module (reuses L0; READ-ONLY going forward)
repro/wave_world_model_results.json     ← S7 results (digest 41e81a7f…)
repro/wave_world_model_atlas.png        ← S7 figure
repro/make_figure_v0_7.py               ← S7 figure generator
repro/expected_digest_v0_7.json         ← S7 pinned digest
repro/wave_embodiment_core.py           ← S8 module (reuses L0+L6; READ-ONLY going forward)
repro/wave_embodiment_results.json      ← S8 results (digest 17aa27bf…)
repro/wave_embodiment_atlas.png         ← S8 figure (8 panels)
repro/make_figure_v0_8.py               ← S8 figure generator
repro/expected_digest_v0_8.json         ← S8 pinned digest
repro/wave_workspace_core.py            ← S9 module (reuses L0+L3 gate; READ-ONLY going forward)
repro/wave_workspace_results.json       ← S9 results (digest 52a0ce34…)
repro/wave_workspace_atlas.png          ← S9 figure (4 panels)
repro/make_figure_v0_9.py               ← S9 figure generator
repro/expected_digest_v0_9.json         ← S9 pinned digest
repro/wave_agi_core.py                  ← S10 module = L9 END CONDITION (reuses L0+L1+L3; READ-ONLY going forward; checkpoint-cached to .cache_v0_10/ on a 1-core box)
repro/wave_agi_results.json             ← S10 results (digest 20b5f2c2…; 6/7 ladder rungs [V])
repro/wave_agi_atlas.png                ← S10 figure (8 panels: 7 rungs + ladder verdict)
repro/make_figure_v0_10.py              ← S10 figure generator
repro/expected_digest_v0_10.json        ← S10 pinned digest
repro/wave_adapt_closure_core.py        ← S11 module = POST-PROGRAM HARDENING, A3 [O]→[V] (reuses frozen L0 + the inherited R3/C2 episodic field; READ-ONLY going forward)
repro/wave_adapt_closure_results.json   ← S11 results (digest ad93057a…; A3_closure_O_to_V=True, ladder 7/7 [V] with the dual store wired in)
repro/wave_adapt_closure_atlas.png      ← S11 figure (4 panels: closure · mechanism-not-norm · size-robustness · verdict)
repro/make_figure_v0_11.py              ← S11 figure generator
repro/expected_digest_v0_11.json        ← S11 pinned digest
repro/wave_axiom_audit_core.py          ← S12 module = POST-PROGRAM COMPRESSION, axiom-independence audit (reuses frozen L0 via broken substitutes built beside it; L0 never edited; READ-ONLY going forward)
repro/wave_axiom_audit_results.json     ← S12 results (digest 7f59ced6…; 8→5 axioms, all 5 load-bearing, axiom_set_irreducible=True)
repro/wave_axiom_audit_atlas.png        ← S12 figure (6 panels: compression map · intact control · the five ablations · verdict)
repro/make_figure_v0_12.py              ← S12 figure generator
repro/expected_digest_v0_12.json        ← S12 pinned digest
repro/check_completeness.py             ← self-containment + reproduction audit
```
New sessions append their own `SESSION_v0_k_*.md`, `repro/<new_module>.py`,
`repro/<new_results>.json`, `repro/<new_figure>.png`, `repro/expected_digest_v0_k.json`
to this list (and update the manifest in this file).

**Verify intactness at any time:**
```
python3 repro/check_completeness.py
```
It checks every manifest file exists, re-runs all twelve sims, and compares digests
bit-for-bit (the S10 L9 end-condition module is the heaviest; on a single core it
checkpoint-caches its 7 ladder rungs so re-runs resume — determinism unchanged; the S11
hardening module and the S12 compression-audit module are both light). If it
fails, the package has regressed — fix before doing new work.

---

## 5. Invariant rules (every session)

1. **English only**, going forward.
2. **One zip, additive, no fresh tree, nothing dropped.** Contains cumulative
   history + this session's results + the updated blueprint.
3. **Frozen substrate L0 is READ-ONLY.** Upper layers reuse it; they never modify it
   (mirrors the brain project's frozen-engine discipline).
4. **No tuning.** No value is fit to a target. Inherited numbers are principles, not
   fitting targets. `new_tuned_constants = 0` per module.
5. **Stress Principle.** Every new claim must pass a test **designed to break it** to
   earn `[V]`/`[L]`. If it breaks, record the break and restart that line with the
   break applied. Collapses are preserved, never erased.
6. **Non-circular read-outs**; **sign-stable across sweeps**; **deterministic**
   (digests reproduce).
7. **Firewall.** Function only. The hard-problem blank stays open and is never
   erased.
8. **Physical realization deferred.** Theory track now.

---

## 6. Output contract (what you must deliver)

Exactly **one zip**, named `vp_wave_computer.zip`, evolved additively from this one,
containing the full file manifest (§4) plus this session's additions, with:
- `check_completeness.py` passing (all files present, both/all digests reproduce),
- the new layer graded and any collapse recorded,
- `CUMULATIVE_LOG.md`, `START_HERE`, and `BLUEPRINT` updated,
- and **no existing file dropped or silently changed**.

Then stop. Self-containment means the next session can repeat this loop from your zip
alone.

---

*This is an in-silico model + theory blueprint — not validated engineering, not a
built device. It is a new long-term program built atop two frozen whitepapers
(`vp_frontal v2`, `vp_physics v0.11.0`), whose inherited content is fully captured
here so this archive stands alone.*
