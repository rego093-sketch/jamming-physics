# COMPLETENESS MANIFEST (누락금지) — vp_eye_emergence_seed

Every artifact this seed promises. `tools/verify_seed.py` fails if any is missing.

- [x] `README.md`
- [x] `VERSION`
- [x] `seed.json`
- [x] `BLUEPRINT.md`
- [x] `FIREWALL.md`
- [x] `INHERITANCE_LEDGER.md`
- [x] `COMPLETENESS_MANIFEST.md`
- [x] `WORK_HANDOVER.md`
- [x] `VP_SPEC_v1_8.md`
- [x] `tools/verify_seed.py`
- [x] `tools/fetch_promoter_gamma.py`
- [x] `inherited/vp_substrate.py`
- [x] `inherited/gamma_pipeline.py`
- [x] `inherited/dna_interpreter.py`
- [x] `inherited/key_pipeline_full.py`
- [x] `inherited/vp_dna_reading.py`
- [x] `inherited/eye_promoters.cache.json`
- [x] `inherited/organ_gamma.json`
- [x] `inherited/FROZEN_SHA256.json`
- [x] `research/E1-angle-to-cone/START_HERE.md`
- [x] `research/E1-angle-to-cone/run.py`
- [x] `research/E1-angle-to-cone/gate_E1.py`
- [x] `research/E2-single-photon-switch/START_HERE.md`
- [x] `research/E2-single-photon-switch/run.py`
- [x] `research/E2-single-photon-switch/gate_E2.py`
- [x] `research/E3-image-formation/START_HERE.md`
- [x] `research/E3-image-formation/run.py`
- [x] `research/E3-image-formation/gate_E3.py`
- [x] `research/E4-congenital-blindness/START_HERE.md`
- [x] `research/E4-congenital-blindness/run.py`
- [x] `research/E4-congenital-blindness/gate_E4.py`
- [x] `research/E5-frequency-ladder/START_HERE.md`
- [x] `research/E5-frequency-ladder/run.py`
- [x] `research/E5-frequency-ladder/gate_E5.py`
- [x] `research/E6-why-visible/START_HERE.md`
- [x] `research/E6-why-visible/run.py`
- [x] `research/E6-why-visible/gate_E6.py`
- [x] `research/E7-cascade-lowpass/START_HERE.md`
- [x] `research/E7-cascade-lowpass/run.py`
- [x] `research/E7-cascade-lowpass/gate_E7.py`
- [x] `research/E8-graded-to-spike-rate/START_HERE.md`
- [x] `research/E8-graded-to-spike-rate/run.py`
- [x] `research/E8-graded-to-spike-rate/gate_E8.py`
- [x] `research/E9-red-green-dichromacy/START_HERE.md`
- [x] `research/E9-red-green-dichromacy/run.py`
- [x] `research/E9-red-green-dichromacy/gate_E9.py`
- [x] `research/E10-light-dark-adaptation/START_HERE.md`
- [x] `research/E10-light-dark-adaptation/run.py`
- [x] `research/E10-light-dark-adaptation/gate_E10.py`
- [x] `research/E11-accommodation-refraction/START_HERE.md`
- [x] `research/E11-accommodation-refraction/run.py`
- [x] `research/E11-accommodation-refraction/gate_E11.py`
- [x] `research/E12-acquired-degeneration/START_HERE.md`
- [x] `research/E12-acquired-degeneration/run.py`
- [x] `research/E12-acquired-degeneration/gate_E12.py`
- [x] `inherited/vp_light_emergence_quantum.py`
- [x] `inherited/vp_color_by_angle.py`
- [x] `inherited/vp_visible_band_canonical.py`
- [x] `BUILD_VOLUME.md`
- [x] `tools/_volume_lib.py`
- [x] `tools/build_volume.py`
- [x] `tools/gate_volume.py`
- [x] `docs/eye/index.html`
- [x] `docs/eye/assets/volume.css`
- [x] `docs/eye/sitemap.xml`
- [x] `docs/eye/robots.txt`
- [x] `docs/eye/llms.txt`
- [x] `docs/eye/e0-carrier/index.html`
- [x] `docs/eye/e1-angle-to-cone/index.html`
- [x] `docs/eye/e2-single-photon-switch/index.html`
- [x] `docs/eye/e3-image-formation/index.html`
- [x] `docs/eye/e4-congenital-blindness/index.html`
- [x] `docs/eye/e5-frequency-ladder/index.html`
- [x] `docs/eye/e6-why-visible/index.html`
- [x] `docs/eye/e7-cascade-lowpass/index.html`
- [x] `docs/eye/e8-graded-to-spike-rate/index.html`
- [x] `docs/eye/e9-red-green-dichromacy/index.html`
- [x] `docs/eye/e10-light-dark-adaptation/index.html`
- [x] `docs/eye/e11-accommodation-refraction/index.html`
- [x] `docs/eye/e12-acquired-degeneration/index.html`
- [x] `docs/eye/facts/e0-carrier.json`
- [x] `docs/eye/facts/e1-angle-to-cone.json`
- [x] `docs/eye/facts/e2-single-photon-switch.json`
- [x] `docs/eye/facts/e3-image-formation.json`
- [x] `docs/eye/facts/e4-congenital-blindness.json`
- [x] `docs/eye/facts/e5-frequency-ladder.json`
- [x] `docs/eye/facts/e6-why-visible.json`
- [x] `docs/eye/facts/e7-cascade-lowpass.json`
- [x] `docs/eye/facts/e8-graded-to-spike-rate.json`
- [x] `docs/eye/facts/e9-red-green-dichromacy.json`
- [x] `docs/eye/facts/e10-light-dark-adaptation.json`
- [x] `docs/eye/facts/e11-accommodation-refraction.json`
- [x] `docs/eye/facts/e12-acquired-degeneration.json`

**Total: 93 artifacts.** Foundation modules (run twice for determinism by `verify_seed.py` [2]):
`inherited/vp_light_emergence_quantum.py`, `inherited/vp_color_by_angle.py`,
`inherited/vp_dna_reading.py`, **`inherited/vp_visible_band_canonical.py`** (visible-band canon, added v0.7.0),
**`research/E1-angle-to-cone/run.py`** (E1, added v0.3.0),
**`research/E2-single-photon-switch/run.py`** (E2, added v0.4.0),
**`research/E3-image-formation/run.py`** (E3, added v0.5.0),
**`research/E4-congenital-blindness/run.py`** (E4, added v0.6.0),
**`research/E5-frequency-ladder/run.py`** (E5, added v0.8.0),
**`research/E6-why-visible/run.py`** (E6, added v0.9.0),
**`research/E7-cascade-lowpass/run.py`** (E7, added v0.10.0),
**`research/E8-graded-to-spike-rate/run.py`** (E8, added v0.11.0),
**`research/E9-red-green-dichromacy/run.py`** (E9, added v0.13.0),
**`research/E10-light-dark-adaptation/run.py`** (E10, added v0.14.0),
**`research/E11-accommodation-refraction/run.py`** (E11, added v0.15.0),
**`research/E12-acquired-degeneration/run.py`** (E12, added v0.16.0).

**v0.6.0 inherited update (deliberate, logged):** the atlas + cache grew from 16 → **19** master
genes — the three deferred congenital-blindness genes **AIPL1, RPGR, PDE6B** were fetched
(`tools/fetch_promoter_gamma.py`, measured γ), folded into `inherited/eye_promoters.cache.json` and
`inherited/organ_gamma.json`, and those two artifacts were **re-frozen** in
`inherited/FROZEN_SHA256.json` (see `INHERITANCE_LEDGER.md`). No other inherited artifact changed.

**v0.7.0 inherited addition (deliberate, logged):** one new frozen foundation module —
`inherited/vp_visible_band_canonical.py` — was brought in from **vp_physics v0.11.0** (§9.4 quantum
diameter D, §10.9 light-propagation-angle band table, §11.2–11.6 unit-realization + the **RCROSS(633/532)**
two-channel cross-check). It is a faithful reproduction of the FORCED relations (recomputed bit-exact
from D; unit-realization chain quoted from source) and adds **no biology and no tuning**. The total
count is unchanged at **34**: this addition is offset by removing a stale duplicate `inherited/vp_dna_reading.py`
entry that had been listed twice (it remains present, listed once). The module is registered in
`inherited/FROZEN_SHA256.json` (now 10 frozen files) and runs twice for determinism in `verify_seed.py [2]`.

**v0.8.0 increment (E5 — the frequency ladder):** three new artifacts —
`research/E5-frequency-ladder/{run.py, gate_E5.py, START_HERE.md}` — were added (total 34 → **37**). E5
quantifies the high→low down-conversion: ν=c/λ ≈ 10¹⁴ Hz carrier → ~13-order collapse → a
carrier-frequency-invariant low-pass band set by the recovery τ, demonstrated on the FROZEN Neuron and
the inherited rung-1 canon. It adds **no γ and no tuning** (γ(GUCY2D) read byte-equal from the atlas,
ν/E forced, SEED=19) and makes no clinical claim. `run.py` is in the verifier's foundation list (runs
2× for determinism); `gate_E5.py` is 8 independent checks (`E5 GATE: PASS`).

**v0.9.0 increment (E6 — why the band is visible):** three new artifacts —
`research/E6-why-visible/{run.py, gate_E6.py, START_HERE.md}` — were added (total 37 → **40**). E6
answers *why* the carrier is the visible band: two unrelated constraints select the same window —
GATE (i) the inherited §10.9 geometric placement (visible sandwiched in m between x-ray and infrared,
near-transverse) [F], and GATE (ii) the photochemical energy window (reversible 11-cis→all-trans
isomerisation ~1.8–3.3 eV → 375.71–688.80 nm) [L]. The honest finding is that **geometry only PLACES
the band and the chromophore energy PINS it** — the angle is a smooth sawtooth that never gates a band,
so the edges are photochemical, not geometric; the deep-red edge is soft and the chromophore tuning is
a named **[O]** (no lever from the promoter-γ). **Precision discipline:** the wavelength is the anchor,
mapped EXACTLY to ν=c/λ, period T=1/ν, and E=hν with the SI-exact constants (no rounding), using
already-measured visible wavelengths (the RCROSS channels 633/532 nm and the cone-opsin λmax
420/530/560 nm); angles are carried to full precision (χ to 10 dp + the near-grazing deficit) and each
satisfies the closure m·sinχ·D/λ=1 to 1e-15. It adds **no γ and no tuning** (D byte-equal to the
inherited invariant, c/h SI-exact, SEED=19) and makes no clinical claim. `run.py` is in the verifier's
foundation list (runs 2× for determinism); `gate_E6.py` is 8 independent checks (`E6 GATE: PASS`).

**v0.10.0 increment (E7 — the cascade as the band-setting low-pass):** three new artifacts —
`research/E7-cascade-lowpass/{run.py, gate_E7.py, START_HERE.md}` — were added (total 40 → **43**). E7
turns E5's qualitative "the carrier is averaged away" into an **explicit, measured filter**. The FROZEN
Neuron's recovery law (`w += dt*(s − β·w)/τ_s`, quoted verbatim) **is** a first-order leaky integrator,
i.e. a single-pole low-pass with transfer function H(f)=1/(β+i·2πf·τ_s). The one claim — **the output band
is set by the recovery time-constant, f_c = β/(2πτ_s)** — is read off the FROZEN substrate four ways
that all agree: DC gain = 1/β = 2.000 [F]; the measured cutoff matches β/(2πτ) to 0.16% [V]; the phase
lag at the cutoff is 44.99° (a single pole ⇒ 45°) [V]; the high-frequency roll-off is −19.96 dB/decade
(one pole ⇒ −20) [V]. Sweeping τ∈{20,40,80} shows **f_c ∝ 1/τ exactly** (f_c·τ = β/2π = 0.07958 held
constant to 0.27%), and the band is invariant to both the carrier frequency and the promoter-γ — it is
**τ, not the carrier and not γ, that fixes the band**. The full FROZEN neuron then inherits this: its
emergent dominant rhythm falls monotonically in τ with range(τ) ≫ range(γ), so γ is a **secondary**
mover (via dwell ∝ γ^1.5) at fixed τ. The honest scope: the full-neuron rhythm is only **approximately**
∝1/τ (a relaxation period = slow τ-recovery + a τ-independent fast transit), preserved as a named
**[O]**, together with the single-pole idealisation vs. the real multi-stage cascade
(rhodopsin→transducin→PDE→cGMP→CNG) and the absolute τ→Hz calibration. It adds **no γ and no tuning**
(Neuron β=0.5/τ_f=1.0/τ_s=40.0 byte-equal, γ(GUCY2D)=1.365 read byte-equal from the atlas, the filter law
quoted verbatim, SEED=19) and makes no clinical claim. `run.py` is in the verifier's foundation list
(runs 2× for determinism); `gate_E7.py` is 8 independent checks (`E7 GATE: PASS`).

**v0.11.0 increment (E8 — graded → spike-rate re-quantisation; the ladder's last rung):** three new
artifacts — `research/E8-graded-to-spike-rate/{run.py, gate_E8.py, START_HERE.md}` — were added (total
43 → **46**). E8 is the final rung of the down-conversion ladder: the retina re-encodes E7's **graded**
~Hz transduction signal as a discrete ganglion-cell spike **RATE** (rate coding). The re-quantiser is the
FROZEN R19 Neuron itself — the same neuron whose slow recovery set the band in E7 now **fires**, each
spike a membrane up-crossing = an all-or-none fold-crossing event (inherited `spikes`/`rate_hz`/
`dominant_freq`). Measured on the FROZEN substrate, the rate code is **thresholded** (a rheobase floor:
silent until the switch is driven across its fold), **bounded** (a depolarisation-block ceiling — the
full-range f–I is a *bandpass in drive*, silent on BOTH sides), **monotone** (the rate rises strictly with
the graded amplitude across the operating band), and a near-periodic clock (CV(ISI)≈0.0002). The central
result — **the carrier stays gone (no mixing)** — closes the loop with E5/E7: hold the slow envelope fixed
and sweep the carrier ⇒ the rate is invariant (spread 1.53% across a 20× span, rate ⟂ carrier); raise the
envelope ⇒ the rate rises (rate ∝ envelope); the spike train sits in the neural low band (dominant freq =
rate, orders below the carrier), so the ~13-order down-conversion is preserved end-to-end. γ is shown
READ-ONLY as a structural excitability OFFSET (it shifts the rheobase, not the band or the signal), with
**no** "γ negligible" claim made. The **felt percept** of sight is OUT OF SCOPE — it hands off to the
**mind** volume (firewall). It adds **no γ and no tuning** (Neuron β=0.5/τ_f=1.0/τ_s=40.0 byte-equal,
γ(GUCY2D)=1.365 read byte-equal from the atlas, the spike reader inherited verbatim, the operating point
an input not a fitted target, SEED=19) and makes no clinical claim. `run.py` is in the verifier's
foundation list (runs 2× for determinism); `gate_E8.py` is 8 independent checks (`E8 GATE: PASS`). Named
[O]: the absolute Hz/spike-count, the f–I shape, the single-stage collapse of the retinal network, and the
felt percept (→ mind volume). **With E8 the down-conversion spine E0→E8 is complete.**

**v0.12.0 increment (the multi-chapter HTML volume — the deliverable that publishes E0→E8):**
thirty new artifacts — `BUILD_VOLUME.md`, `tools/{_volume_lib.py, build_volume.py, gate_volume.py}`,
and the rendered volume under `docs/eye/` (a hub `index.html`, nine chapter pages `e0…e8/index.html`,
nine machine-readable `facts/*.json`, plus `assets/volume.css`, `sitemap.xml`, `robots.txt`, `llms.txt`)
— were added (total 46 → **73**). This rung adds **no physics and no tuning**: it is the *publication*
layer over the finished down-conversion spine. Every displayed number on every page is the **code's own
output** — `build_volume.py` runs each chapter's `run.py` (the single source), embeds its **verbatim**
transcript hash-pinned inside the page, and routes every number that also appears in prose through a
build-time gate `F()` that ASSERTS the exact string is a substring of that live transcript, so a typo'd or
invented number fails the build. `tools/gate_volume.py` then re-proves the deliverable **independently of
the builder** with seven checks (`VOLUME GATE: PASS`): **G1** each `run.py` is byte-identical across two
runs; **G2** the live transcript sha equals the sha recorded in `facts/*.json`; **G3** the `<pre>`
transcript embedded in each page, un-escaped, is byte-identical to the live run (sha three ways) — so the
HTML shows the *real* run, **drift 0**; **G4** every surfaced number is present verbatim in BOTH the live
transcript AND the rendered HTML; **G5** the rendered **E4** disease chapter carries **none** of E4's own
`MAGNITUDE_BLOCK` tokens and no `%` (the firewall holds in the *prose*, proposal-only); **G6** every page
is static and machine-readable (`<h1>`, `<link rel=canonical>`, a JSON-LD block, and **every** `<script>`
is `type=application/ld+json` — no executable JS); **G7** every file in `expected_files()` exists. The gate
is **non-vacuous** (verified): corrupting a prose number trips G4, tampering one transcript byte trips G3.
The volume is retrieval-ready (answer-first leads, self-contained passages, sitemap/robots/llms). The
official live-site merge (canonical `paper_id`, 301 redirects, cross-volume DOI registry) is a separate
VP-SPEC Phase 5 step and stays out of this package's lane. **The Eye volume now ships as one zip.**

**v0.13.0 increment (E9 — red-green colour vision; the first mechanism extension beyond the spine):**
three new research artifacts — `research/E9-red-green-dichromacy/{run.py, gate_E9.py, START_HERE.md}` —
plus two rendered docs (`docs/eye/e9-red-green-dichromacy/index.html` and its
`docs/eye/facts/e9-red-green-dichromacy.json`) were added (total 73 → **78**). E9 is the **first chapter
past the completed E0→E8 down-conversion spine**: instead of carrying the carrier further down, it asks
what the FROZEN E1 angle map already FORCES about colour discrimination, and why the most common inherited
colour-vision difference is **red-green**. Colour is propagation angle χ(λ) (inherited E1/E6), so the three
cones are three angle-samples and the three discrimination axes are the three pairwise angle **margins**,
read byte-equal off the frozen `chi_deg` at the measured cone λmax (S 420, M 530, L 560 nm): S-M = 0.0509°,
**M-L (green-red) = 0.0421° — the SMALLEST**, S-L = 0.0931° (M-L is 1.208× tighter than S-M). The headline
[F]↔[L] is that **the angle map makes green-red the structurally most fragile colour axis, matching the
epidemiology** — with no fitting and no new γ. What is FORCED (not tuned): **(1) coincidence ⇒ collapse** —
χ is a function of λ alone, so two opsins at the same λmax map to the same angle and the margin is **exactly
0.000000°**; bringing a hypothetical L′ down toward λ(M) drives the margin monotonically to exactly 0
(sawtooth-proof: the limit of convergence *is* the loss), unifying **dichromacy** (remove one angle-sample,
the within-red-green axis vanishes, exactly one chromatic axis survives) and **anomalous trichromacy**
(peaks converge, margin → 0) as ONE continuum. **(2) A direction-only lever** — peaks toward each other ⇒
margin shrinks; peaks apart ⇒ margin grows; the magnitude is a named **[O]** (firewalled). **(3)
Orthogonality** — colour (angle, E1) ⟂ position (image, E3) ⟂ brightness (the R19 switch, E2); the remaining
cones' switch is shown spinodal-stable (untouched), so losing one cone costs **exactly one colour axis**
while acuity and single-photon detection stay intact — which is *why* it is colour-blindness, not blindness.
**(4) γ is read READ-ONLY** (OPN1LW γ=1.4820, OPN1MW γ=1.4058, Δγ=0.0762, A4 shapes differing 1.40×) as a
structural offset, with **no** claim that γ predicts λmax; the X-linked OPN1LW/OPN1MW tandem array is named
[L] genomics, not γ. The chapter is **condition-direction-only and proposal-only**: it is the package's
**second firewalled clinical chapter** (after E4), and `run.py` carries its own `MAGNITUDE_BLOCK` whose
`main()` ASSERTS the transcript holds none of those tokens and no `%`. It adds **no γ and no tuning** (cone
angles byte-equal from the frozen angle map, λmax already-measured, SEED=19) and makes no efficacy claim;
the **felt percept** of colour is OUT OF SCOPE (→ mind volume). `run.py` is in the verifier's foundation
list (runs 2× for determinism); `gate_E9.py` is 8 independent checks (`E9 GATE: PASS`); the rendered chapter
is re-proven by `gate_volume.py` (every E9 number ∈ its live transcript, drift 0, firewall held in prose).
**The foundation does not move:** E9 fetched nothing and re-froze nothing — `inherited/FROZEN_SHA256.json`
is byte-identical to v0.12.0 (see `INHERITANCE_LEDGER.md`).

**v0.14.0 increment (E10 — light/dark adaptation as gain control; the second mechanism extension):**
three new research artifacts — `research/E10-light-dark-adaptation/{run.py, gate_E10.py, START_HERE.md}` —
plus two rendered docs (`docs/eye/e10-light-dark-adaptation/index.html` and its
`docs/eye/facts/e10-light-dark-adaptation.json`) were added (total 78 → **83**). E10 is a **NORMAL,
non-clinical mechanism** chapter (like E5–E8, *not* a disease chapter — it carries no `MAGNITUDE_BLOCK`
and no firewall): it asks what the FROZEN R19 switch already FORCES about adaptation, i.e. how the eye
spans ~10 decades of background light with a bounded response. The mechanism is **the switch's own
saturating ON branch**: E2's bistable field `γ·s − s³ + h = 0`, on its ON branch, is the real root of
`s³ − γ·s − h = 0`, and for a large background drive h the cubic dominates ⇒ the steady state
**s*(h) → h^(1/3)** — a compressive (cube-root) response, read off the FROZEN substrate four ways that
all agree. **(A) Dynamic-range compression** — `s*/h^(1/3)` falls monotonically to 1 (saturation onto the
cube root; every s* a true zero of the frozen field, max residual 2.3e-13 [V]); a **5.000000-decade**
background sweep (1→10⁵) fits a **1.500207-decade** response (compression **3.332873**), and the deep
sweep 100→10⁸ gives 6.000000 → 1.990223 decades ⇒ the cubic order **n=3** [F]. **(B) Automatic gain
control** — the incremental gain `ds*/dh = 1/(3s*²−γ)` falls monotonically dim→bright (2.004737e-01 at
h=1 → 1.546844e-04 at h=10⁵, a **1296.0×** turn-down) with **no separate machinery invoked** — saturation
*is* the gain control (gain ∝ h^(−2/3)) [F]. **(C) the headline — a Weber-Fechner-like law, FORCED not
fitted:** the contrast gain (response to a *fractional* change) `(h/s*)·ds*/dh` rises to a constant that
the pure-cube limit pins **EXACTLY at 1/n = 1/3 = 0.333333** for any h (s*=2.000000@h=8, 10.000000@h=1000,
1000.000000@h=10⁹), and it is **γ-INDEPENDENT** — RHO (γ=1.4719) gives 0.333301 and CNGB3 (γ=1.2425) gives
0.333306 at h=10⁶, both → 1/3 — so equal fractional steps feel equal regardless of the gene; the
compression is the cubic **order**, not the threshold γ. The honest scope is stated, not hidden: this is
the **saturated tail**; near the fold (small h) the γ·s term matters and the response is steeper than a
clean power law. **(D) two regimes** — **DARK** = operating point near the fold h*(γ)=0.68733 (highest
gain + E2's single-photon flip), **BRIGHT** = driven deep into the cube root (low gain, compressed against
saturation), adaptation being the operating point **sliding** fold→cube-root. **γ is READ-ONLY** (it sets
the fold/offset where dark sensitivity sits; the contrast→1/3 law is γ-independent — an OFFSET, as in
E7/E8), and the dark re-sensitisation **recovery runs on the FROZEN Neuron's τ_s=40.0 (β=0.5)** — the same
time-constant that set the band (E7) and the rate code (E8); structure [F], absolute seconds the inherited
**[O]**. It adds **no γ and no tuning** (substrate `sdot`/`spinodal`/Neuron byte-equal, γ(RHO)/γ(CNGB3)
read byte-equal from the atlas, the steady state solved by deterministic Newton with machine-checked
residual, SEED=19) and makes **no clinical claim**; the **felt brightness percept** is OUT OF SCOPE
(→ mind volume). `run.py` is in the verifier's foundation list (runs 2× for determinism); `gate_E10.py`
is 8 independent checks (`E10 GATE: PASS`); the rendered chapter is re-proven by `gate_volume.py` (every
E10 number ∈ its live transcript, drift 0). **The foundation does not move:** E10 fetched nothing and
re-froze nothing — `inherited/FROZEN_SHA256.json` is byte-identical to v0.12.0 and v0.13.0 (see
`INHERITANCE_LEDGER.md`).

**v0.15.0 increment (E11 — accommodation & refractive error as the power↔length match; the third
mechanism extension):** three new research artifacts —
`research/E11-accommodation-refraction/{run.py, gate_E11.py, START_HERE.md}` — plus two rendered docs
(`docs/eye/e11-accommodation-refraction/index.html` and its
`docs/eye/facts/e11-accommodation-refraction.json`) were added (total 83 → **88**). E11 is a
**CLINICAL/condition** chapter (myopia/hyperopia/presbyopia) — like E9 it is in `DISEASE_CHAPTERS`, so the
`gate_volume.py` **G5** firewall covers its rendered HTML (and its own `gate_E11.py` carries **G8**). It
consumes E3's FROZEN single-surface optics **read-only** (via `importlib`, never re-derived) and asks what
that frozen eye already FORCES about focus. E3 images a distant object at **v_∞ = n₂/P** with
**P = (n₂−n₁)/R**; the image lands on the retina ⇔ **P·L = n₂**, so everything turns on the dimensionless
**match ratio ρ ≡ P·L/n₂**, read off four ways that all agree. **(A) Emmetropia is ρ = 1** — the frozen
Emsley reduced eye sits **exactly** there (**ρ = 1.000000**), so focus is a *whole curve* in the
power×length plane, not one privileged eye [V-arith]. **(B) Refractive error is sign(ρ−1)** — **ρ =
1.050000 > 1** puts the distant focus *in front* of the retina ⇒ **MYOPIA**; **ρ = 0.950000 < 1** *behind*
⇒ **HYPEROPIA**; and each is reachable **two ways**, an eye too long OR too powerful, because **only the
product P·L matters** (a too-strong eye `q·ℓ = 1` shortened to compensate returns **ρ = 1.000000**) —
stated **direction-only**, the dioptric magnitude a named **[O]**. **(C) Accommodation is a one-signed
power lever** — a near object at `u = −k·L` needs `P_req/P_∞ = 1 + n₁/(n₂·k)`, which rises
**monotonically** as the object nears (**1.000750 → 1.007500 → 1.030000 → 1.075000 → 1.187500 →
1.375000**) and is **always ≥ 1**, supplied by **rounding the lens** (R↓ ⇒ P↑; `R_req/R_∞` nearest
**0.727273** re-focuses on the retina); accommodation amplitude and absolute near-distances **[O]**.
**(D) The lever is lattice geometry** — power rides curvature **dP/P = −dR/R** (P ∝ 1/R; the same √(B/ρ)
refractive-index rule as E3), while the *length* axis is the substrate **dwell size-law size ∝ γ^1.5**
(E1) — PAX6 read-only (PAX6/RAX dwell ratio **1.059595**) instantiates the *direction* of the growth
shift (more eye-field growth ⇒ longer eye ⇒ myopic), the gene→axial-elongation map itself an **[O]**. It
adds **no γ and no tuning** (E3 optics imported read-only, substrate `dwell`/`spinodal` byte-equal,
γ(PAX6)/γ(RAX) read byte-equal from the atlas, SEED=19, deterministic 2×SHA-256) and makes **no clinical
claim**: E11's `MAGNITUDE_BLOCK` mirrors E9's and **adds** `diopter`/`dioptre` and bare `mm`, so every
fact is a dimensionless ratio or a sign — `gate_E11.py` G8 and `gate_volume.py` G5 both confirm no
magnitude token and no "%". The **felt blur/clarity percept** is OUT OF SCOPE (→ mind volume). `run.py`
is in the verifier's foundation list (runs 2× for determinism); `gate_E11.py` is 8 independent checks
(`E11 GATE: PASS`); the rendered chapter is re-proven by `gate_volume.py` (every E11 number ∈ its live
transcript, drift 0, firewall held in prose). **The foundation does not move:** E11 fetched nothing and
re-froze nothing — `inherited/FROZEN_SHA256.json` is byte-identical to v0.12.0, v0.13.0 and v0.14.0 (see
`INHERITANCE_LEDGER.md`).

**v0.16.0 increment (E12 — acquired/degenerative disease as the R19 switch carried OVER its fold, with
hysteresis; the fourth mechanism extension):** three new research artifacts —
`research/E12-acquired-degeneration/{run.py, gate_E12.py, START_HERE.md}` — plus two rendered docs
(`docs/eye/e12-acquired-degeneration/index.html`, `docs/eye/facts/e12-acquired-degeneration.json`) were
added (total 88 → **93**). E12 is a **CLINICAL/condition** chapter (the late picture shared across AMD,
glaucoma and diabetic retinopathy), so it joins `DISEASE_CHAPTERS` and the `gate_volume.py` **G5** firewall
covers its rendered HTML (and its own `gate_E12.py` carries **G8**). The honest physics is a saddle-node
collapse with hysteresis on the **frozen** R19 field `ds/dt = γ·s − s³ + h`, distinct from E4 in **four**
parts: **(A) degeneration = a slow drift over the fold** — a switch that *started* healthy (settles ON,
**s = 1.316420**, on RHO γ **1.4719**, fold h\* **0.687330**) tracks the upper branch down and **collapses
in one step** at **h = -0.691855 ≈ −h\* = -0.687330** (a *tipping point*, start-independent: a different
start collapses at **-0.689701**) — categorically unlike E4's switch *born below* its fold (static, never
flips). **(B) Hysteresis** — recovery comes only at the *opposite* fold **+h\* = 0.687330** (drive
**0.691855**), loop width **1.383710 ≈ 2·h\* = 1.374661** = the irreversibility margin (early ≠ late).
**(C) One geometry, many routes** — a **load route** (lower h to −h\*) and a **basin-shallowing route**
(erode γ under fixed load **-0.300000** until the shrinking fold **h\*(γ) = 0.296976** meets it at **γ =
0.841231**, analytic γ_crit **0.846932**) **both land on the one locus h = −h\*(γ)** (gap **0.003024**), so
the shared late picture across the three diseases is *forced* while each primary stress's identity is a
named **[O]**. **(D) γ as STRUCTURAL fragility** — lower γ ⇒ shallower basin ⇒ tips under less stress
(spinodal-monotone CNGB3 **h\* = 0.533080** → PDE6B **h\* = 0.732285**, span **1.373687**), READ-ONLY and
byte-equal to the atlas, with the **loudest caveat in the volume**: the disease-gene→γ map is *even more*
**[O]** than E4 (polygenic, age/environment-gated, mostly non-promoter common variants — the promoter-γ has
essentially **no monogenic purchase** here). It adds **no γ and no tuning** (substrate `sdot`/`spinodal`/
`barrier` imported read-only, atlas γ byte-equal, SEED=19, deterministic 2×SHA-256) and makes **no clinical
claim**: E12's `MAGNITUDE_BLOCK` mirrors E9's and **adds** `hba1c`/`mg/dl`/`mmol/l`/`mmol/dl` and bare
`mm`/`mmhg` guards, so every stress is a dimensionless multiple of the gene's *own* fold — `gate_E12.py` G8
and `gate_volume.py` G5 both confirm no magnitude token and no "%". The **felt loss of sight** is OUT OF
SCOPE (→ mind volume). `run.py` is in the verifier's foundation list (runs 2× for determinism);
`gate_E12.py` is 8 independent checks (`E12 GATE: PASS`); the rendered chapter is re-proven by
`gate_volume.py` (every E12 number ∈ its live transcript, drift 0, firewall held in prose). **The
foundation does not move:** E12 fetched nothing and re-froze nothing — `inherited/FROZEN_SHA256.json` is
byte-identical to v0.12.0 through v0.15.0 (see `INHERITANCE_LEDGER.md`).
