# START HERE — increment E3

The third emergence (see `BLUEPRINT.md` for the full plan). **BUILT (v0.5.0).**

**Task:** image formation — ground the reduced-eye optics on the SAME inherited wave physics that
already carries colour, the **internal angle law** `sinχ = λ/(mD)`, D invariant
(`inherited/vp_light_emergence_quantum.py`, `inherited/vp_color_by_angle.py`; chemistry §1,
conduction↔radiation **by angle**). External Snell/refraction is kept only as the lab-space **shadow**
of that wave physics — never an independent axiom — and every step that is ordinary classical ray
arithmetic is flagged **[V-arith]**, not dressed up as substrate-derived.

**Why the internal angle theory (the productive path):** if refraction is taken externally — "the
medium has a measured index n, apply Snell" — then n is opaque, the focus is pure trigonometry, the
substrate contributes nothing, and colour and image are unrelated. The angle law fixes all three:
(i) what n **is** — the lattice wave-speed ratio `n = c_vac/c_med = √((B/ρ)_vac /(B/ρ)_med)`;
(ii) what Snell **is** — the transverse-oscillation (phase) match at the interface; and (iii) that
refraction conserves the frequency f, so the colour-angle χ(λ) **survives** the optics intact. Same
one wave, two codes.

**Deliverable (done):** a deterministic module `research/E3-image-formation/run.py` that
(A) derives Snell two independent ways — wavefront/**speed** match vs the index form — bit-identical,
and reproduces the total-internal-reflection critical angle (Snell = transverse match; n = the (B/ρ)
ratio); (B) forms the reduced-eye image (single surface, air n₁=1 → eye n₂=4/3, R=5.55 mm — **cited**
Emsley constants) — power ≈ 60 D, a distant point focused at the ≈22.2 mm retina, a real **inverted**
image (m<0, the spatial position code) — honestly flagged **[V-arith]**; (C) shows the optics gives
**WHERE** (a single index co-locates the colours, first-order chromatic split exactly 0) while the
angle law gives **WHAT COLOUR** (χ stays distinct: red 633→89.9378° ≠ green 532→89.8248°), with the
chromatic-aberration **magnitude** a named **[O]** (needs c_med(λ); no diopter number invented). It
prints every displayed number and self-hashes (2× run → identical sha256) and declares grades
[F]/[V]/[V-arith]/[L]/[O] honestly. Plus `gate_E3.py` (`E3 GATE: PASS`), folded into the verifier's
foundation list. The inherited foundation stays **frozen** (E3 adds no γ, edits no inherited artifact).

**Firewall:** structure-only optics; n,R are measured/cited inputs, never fitted; no disease layer,
no dose/efficacy; the felt percept of sight is the mind volume's.

**Next:** `BLUEPRINT.md` → **E4 — congenital BLINDNESS (the goal)**: each congenital-blindness gene —
**GUCY2D, RPE65, CNGB3** (loaded) + **AIPL1, RPGR, PDE6B** (to fetch) — as an R19/transduction
**failure mode** (LOF → the switch cannot flip → no transduction). Direction-only / proposal-only
under `FIREWALL.md`; the research aim is the substrate-inverse lever that could restore the switch —
never a dose or efficacy claim. The felt percept is deferred to the mind volume.
