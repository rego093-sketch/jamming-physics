# START HERE — increment E2

The second emergence (see `BLUEPRINT.md` for the full plan). **BUILT (v0.4.0).**

**Task:** rod phototransduction (**RHO · CNGA1 · CNGB1 · GNAT1**, γ already measured in
`inherited/organ_gamma.json`) as a cooperative **all-or-none** R19 flip — the inherited
`vp_substrate` switch fired by one quantum of drive (one absorbed photon). Show the flip is
**discontinuous** past the spinodal; show the **cubic −s³** is the cooperativity (order n=3) and is
necessary; reuse the **A4 SHAPE** to separate the CNG channel's α/β subunits (degenerate γ). Build
ONLY on the frozen foundation — re-derive nothing, fit nothing.

**Deliverable (done):** a deterministic module `research/E2-single-photon-switch/run.py` that
(a) settles the FROZEN R19 field from rest under a drive sweeping through each gene's spinodal,
(b) demonstrates the discontinuous all-or-none flip, the cubic's necessity, and the A4 in-switch
tie-break, (c) prints every displayed number and self-hashes (2× run → identical sha256), and
(d) declares grades [F]/[V]/[L]/[O] honestly. Plus `gate_E2.py` (`E2 GATE: PASS`), folded into the
verifier's foundation list. The inherited foundation stays **frozen**.

**Firewall:** structure-only; the switch order-parameter `s` is the abstract R19 field, **not** a
photocurrent/voltage/Hz; no dose/efficacy; the felt percept of light is the mind volume's.

**Next:** `BLUEPRINT.md` → **E3 — image formation** (ground the reduced-eye optics on the SAME wave
physics; state honestly what stays classical vs substrate-derived).
