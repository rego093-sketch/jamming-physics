#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
_gen_ch43_spatial_localisation.py — emit the §43 spatial-localisation / field-shaping chapter
(roadmap E1, the v1.50 foundational layer that opens the spatial axis: the scalar global drive
of every prior module generalised to a per-node spatial drive profile).

Writes docs/mind/43-spatial-localisation/index.html using the same head/JSON-LD/footer
scaffolding as the other Part-II chapters so the SEO markup is byte-consistent. The answer-first
<p class="answer"> and the vp-card asides are NOT written here — build_search_layer.py injects them
from mind_registry.py (single source of truth): this generator emits the canonical skeleton
(head + abstract + claim-strip + body sections + nav). Body text is English-only (VP-SPEC C0).

This chapter is the SPATIAL sibling of the §26 E0 plasticity layer. Where E0 added the temporal
core (a slow Hebbian update on the connectivity), E1 adds the spatial core: the engine's single
scalar coupling Kglob — raised or lowered for the whole brain at once in every disorder module
to date — is generalised to a per-node coupling vector Kvec_i = k(b_i)·OMEGA0 (a spatial drive
PROFILE), and a per-node local-coherence field c_i = <sum_j W0_ij cos(theta_j - theta_i)> is read
as a READ-ONLY side computation. The FORM is forced (the frozen ~1/r^3 row-stochastic kernel W0
plus the existing k(b) map, no new constant); the PROFILE (driven node, depth b0) is swept and
every sign/structure holds across a depth sweep b0 in {0.3,0.5,0.7,0.9} (anti-tuning). Four
results hold (E1.1 the field has a fixed spatial structure — local AND normalised, exact; E1.2
perturbation reach is heterogeneous with a drive-stable cerebellar hub; E1.3 the self-localising
vs relay classification is drive-invariant — the relay set {hippocampus, midbrain} fixed at every
amplitude, the headline; E1.4 there is NO universal focal>diffuse law — spatial outcome is site-
determined, the honest no-tuning result), and a uniform drive reproduces the frozen M9 anchor
bit-for-bit (S4). The verifier registers as an atlas citizen (E1-SPATIAL). The firewall is
absolute: a local-coherence field, a reach map and a self/relay class are STRUCTURAL spatial
quantities of the coupling model, never the felt locus of an experience and not a real electrode,
current density or dose (Axis-A; consciousness_claim=0; hard problem OPEN). Only structural signs
and relations are asserted; every magnitude is [O]; nothing is a cure, reversal or prevention;
efficacy=0; not medical advice. The numbers quoted in the prose are the verified reads from
repro/mind/_verify/e1_spatial_localisation.py.

One-shot authoring helper; the emitted HTML is the canonical artifact. The canonical chapter body
is embedded verbatim below (raw string) so re-running reproduces the byte-identical artifact; the
answer and card blocks remain build_search_layer's responsibility (idempotent injection).
"""
import os

HERE = os.path.dirname(os.path.abspath(__file__))
PKG = os.path.abspath(os.path.join(HERE, os.pardir))
MIND = os.path.join(PKG, "docs", "mind")
SLUG = "43-spatial-localisation"
NO = 43
DOI = "10.5281/zenodo.20694404"            # mind paper concept DOI (JSON-LD / footer)
ORCID = "0009-0002-7535-8245"

HTML = r"""<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>Spatial localisation: the scalar global drive generalised to a per-node spatial profile, the field shown to have a fixed structure, and the honest finding that no universal focal-versus-diffuse law exists — Felt Cognition §43 | Jamming Physics</title>
<meta name="description" content="The spatial sibling of the chapter-26 plasticity layer. Every disorder module to date drove the brain globally: one scalar coupling raised or lowered for the whole network at once, so focal-versus-diffuse stimulation, off-target spillover and region-specific disease could not even be posed. This chapter generalises the scalar to a per-node coupling vector — a spatial drive profile — and reads a per-node local-coherence field as a read-only side computation. The drive form is forced (the frozen one-over-r-cubed row-stochastic kernel plus the existing coupling map, no new constant); the spatial profile is swept and every sign holds across a depth sweep. Four results hold. The field has a fixed spatial structure: the kernel is local (every row's weight decreases with anatomical distance) and normalised (every row sums to one), exact to machine precision. Perturbation reach is heterogeneous with a drive-stable hub: driving one node at a time and reading the change in the global order, reach varies more than a hundredfold, and the cerebellum is the rank-one reach hub at every swept depth with the full ranking invariant for moderate-to-high drive. The self-localising versus relay classification is drive-invariant: each node's focal footprint either concentrates on itself or relays to distal sites, and this binary classification is identical across every drive amplitude, the relay set hippocampus and midbrain fixed. And there is no universal focal-beats-diffuse law: delivering equal total dose focally versus diffusely, the sign of the difference varies across sites, so spatial outcome is site-determined and disease built on this layer must be region-specific. A uniform drive reproduces the frozen anchor bit-for-bit. The firewall is absolute: a local-coherence field, a reach map and a self-or-relay class are structural spatial quantities of the coupling model, never the felt locus of an experience and not a real electrode, current density or dose. efficacy=0; not medical advice; no cure, reversal or prevention; the hard problem stays open.">
<link rel="canonical" href="https://jamming-physics.org/mind/43-spatial-localisation/">
<link rel="stylesheet" href="/assets/css/site.css">
<script type="application/ld+json">
{"@context":"https://schema.org","@type":"ScholarlyArticle",
 "headline":"The spatial-localisation / field-shaping layer: the engine's single scalar coupling -- raised or lowered for the whole brain at once in every disorder module to date -- generalised to a per-node coupling vector (a spatial drive profile), with a per-node local-coherence field read as a read-only side computation; the drive form is forced (the frozen ~1/r^3 row-stochastic kernel plus the existing coupling-vs-bias map, no new constant) and the spatial profile is swept so every sign/structure holds across a depth sweep (anti-tuning); four results hold -- the field has a fixed spatial structure (the kernel is local, every row's weight monotone-decreasing with anatomical distance, AND normalised, every row summing to one, exact to machine precision), perturbation reach is heterogeneous with a drive-stable cerebellar hub (driving one node at a time and reading the change in the global order, reach varies more than a hundredfold, the cerebellum is the rank-1 reach hub at every swept depth and the full ranking is invariant for moderate-to-high drive), the self-localising vs relay classification is drive-invariant (each node's focal footprint either concentrates on itself or relays to distal sites, and the classification is identical across every drive amplitude, the relay set hippocampus+midbrain fixed -- the headline), and there is NO universal focal>diffuse law (delivering equal total dose focally vs diffusely the sign of the difference varies across sites, so spatial outcome is site-determined and disease built on this layer must be region-specific, a refuted clean hypothesis reported honestly) -- and a uniform drive reproduces the frozen M9 anchor bit-for-bit; the firewall is absolute, a local-coherence field / reach map / self-or-relay class is a structural spatial quantity of the coupling model, never the felt locus of an experience and not a real electrode, current density or dose (consciousness_claim=0; hard problem OPEN)",
 "isPartOf":{"@type":"CreativeWork","name":"Felt Cognition: Parallel Micro-Eddies, the Stream of Thought, and the Open Problem of Experience",
   "sameAs":"https://doi.org/10.5281/zenodo.20694404"},
 "position":43,
 "author":{"@type":"Person","name":"Young Jae Lee",
   "sameAs":"https://orcid.org/0009-0002-7535-8245"},
 "license":"https://creativecommons.org/licenses/by/4.0/"}
</script>
<script type="application/ld+json">
{"@context":"https://schema.org","@type":"BreadcrumbList","itemListElement":[
 {"@type":"ListItem","position":1,"name":"Home","item":"https://jamming-physics.org/"},
 {"@type":"ListItem","position":2,"name":"Felt Cognition","item":"https://jamming-physics.org/mind/"},
 {"@type":"ListItem","position":3,"name":"\u00a743 Spatial localisation (the field-shaping layer)"}]}
</script>
</head>
<body>
<header><nav class="crumb"><a href="/">Home</a> &rsaquo; <a href="/mind/">Felt Cognition</a> &rsaquo; §43</nav></header>
<main>
<h1>Spatial localisation &mdash; the scalar global drive generalised to a per-node spatial profile, the field shown to have a fixed structure, and the honest finding that no universal focal-versus-diffuse law exists (the field-shaping layer)</h1>
<p class="abstract">Every disorder module so far drove the brain <strong>globally</strong>. From schizophrenia and epilepsy through the <a href="/mind/20-theta-cap-pacemaker/">&theta;-cap</a> to the mood and cycle disorders, the intervention was always a <em>single scalar</em> coupling <span class="mono">K<sub>glob</sub></span> &mdash; one number raised or lowered for the <em>whole network at once</em>. With only a scalar, three questions could not even be posed: <strong>focal versus diffuse</strong> drive, <strong>off-target spillover</strong>, and <strong>region-specific disease</strong>. The fault was always global; the drive had no spatial profile. This chapter adds the <strong>spatial core</strong> &mdash; the spatial sibling of the <a href="/mind/26-plasticity-consolidation/">&sect;26 plasticity layer</a>&rsquo;s temporal core. It generalises the scalar to a <strong>per-node coupling vector</strong> <span class="mono">K<sub>i</sub> = k(b<sub>i</sub>)&middot;&Omega;&#8320;</span> (a spatial drive <em>profile</em>) set through the <em>same</em> <span class="mono">k = &kappa;/(1&minus;|b|)</span> map as the SZ/epilepsy/E0 modules, and reads a <strong>per-node local-coherence field</strong> <span class="mono">c<sub>i</sub> = &lang;&sum;<sub>j</sub> W&#8320;<sub>ij</sub> cos(&theta;<sub>j</sub>&minus;&theta;<sub>i</sub>)&rang;</span> as a <strong>read-only</strong> side computation that never perturbs the &theta; trajectory. The <strong>form is forced</strong> (the frozen <span class="mono">&sim;1/r&sup3;</span> row-stochastic kernel <span class="mono">W&#8320;</span> plus the existing map, <strong>no new constant</strong>); the <strong>profile is swept</strong> and every sign holds across a depth sweep <span class="mono">b&#8320; &isin; {0.3, 0.5, 0.7, 0.9}</span> (anti-tuning). <strong>Four results hold.</strong> <strong>E1.1 &mdash; the field has a fixed spatial structure</strong>: the frozen kernel is <strong>local</strong> (every one of the 12 rows&rsquo; weight <em>monotone-decreases</em> with anatomical distance) and <strong>normalised</strong> (every row sums to 1, deviation <span class="mono">2&times;10<sup>&minus;16</sup></span>) &mdash; exact geometric structure, the substrate of every focal/region claim. <strong>E1.2 &mdash; perturbation reach is heterogeneous with a drive-stable hub</strong>: driving one node at a time and reading the change in the <em>global</em> order, <span class="mono">reach(i) = |R &minus; R&#8320;|</span> varies more than <strong>a hundredfold</strong> (max/min <span class="mono">&gt; 110</span>), and the map is <em>not</em> an artefact of drive amplitude &mdash; the <strong>cerebellum is the rank-1 reach hub at every swept depth</strong> and the full ranking is <strong>invariant for moderate-to-high drive</strong> (<span class="mono">Spearman = 1.0</span> among <span class="mono">b&#8320; &ge; 0.5</span>). <strong>E1.3 &mdash; the self-localising vs relay classification is drive-invariant (the headline)</strong>: each node&rsquo;s focal footprint is either <strong>self-localising</strong> (its own local-coherence change exceeds the mean off-target change) or a <strong>relay</strong> (the change lands harder off-target), and this binary classification is <strong>identical across the entire depth sweep</strong> &mdash; the relay set <span class="mono">{hippocampus, midbrain}</span> fixed at <em>every</em> drive amplitude, the other ten nodes self-localising at every amplitude. Whether a site <em>contains</em> or <em>relays</em> its drive is a <strong>fixed property of where it sits in the field</strong>, not of how hard it is pushed. <strong>E1.4 &mdash; there is no universal focal&gt;diffuse law (the honest no-tuning result)</strong>: a clean, attractive hypothesis &mdash; &ldquo;focal drive always concentrates local gain at its target more than the same dose spread diffusely&rdquo; &mdash; is <strong>false</strong>: delivering equal total dose focally versus diffusely, the sign of <span class="mono">(focal &minus; diffuse)</span> target-gain <strong>varies across sites</strong> (9 of 12 concentrate, the others do not). Spatial <em>outcome</em> is <strong>site-determined</strong>, not governed by a global rule &mdash; which is exactly why disease modules built on this layer must be <strong>region-specific, never global</strong>. A uniform (zero-bias) drive reproduces the frozen <strong>M9 coordination anchor</strong> (<span class="mono">R = 0.38961455156</span>) <strong>bit-for-bit</strong>, and the off-state field equals the baseline exactly &mdash; E1 is a pure <strong>add-on</strong> (engine <span class="mono">0fbf4988&hellip;</span>, byte-unchanged). The <span class="mono">SpatialField</span> class is the <strong>reusable layer</strong> the later region-specific modules import. <strong>The firewall is absolute</strong>: a local-coherence field, a reach map and a self/relay class are <strong>structural spatial</strong> quantities of the coupling model, <strong>never</strong> the felt locus of an experience, and <strong>not</strong> a real electrode, current density or dose (Axis-A &mdash; <span class="mono">consciousness_claim = 0</span>, the hard problem stays <strong>open</strong>). Only structural <em>signs</em> and <em>relations</em> are asserted; every magnitude is <span class="mono">[O]</span>; nothing is a cure, reversal, or prevention; <span class="mono">efficacy = 0</span>; not medical advice.</p>

<aside class="claim-strip">
  <span class="grade g-calibrated">model</span>
  <span class="gate">LOCK → Derive → Gate</span>
  <a href="https://github.com/rego093-sketch/jamming-physics/tree/main/repro/mind/43-spatial-localisation/" rel="noopener">reproduce (GitHub)</a>
  <a href="https://doi.org/10.5281/zenodo.20694404" rel="noopener">DOI snapshot</a>
</aside>
<h2>The global-drive limitation &mdash; what every prior module assumed</h2>
<p>For twenty-five chapters the framework drove the brain with a single number. The engine integrates a network of phase oscillators, one per region, coupled through a frozen ephaptic kernel, and the whole disorder atlas was built by reaching into that integration and turning <em>one</em> dial: the global coupling <span class="mono">K<sub>glob</sub></span>. Schizophrenia lowered it; epilepsy raised it past the over-synchronisation edge; the <a href="/mind/20-theta-cap-pacemaker/">&theta;-cap</a> held it at a paced operating point; the threshold-lever chapters slid it along a reachable axis. Every one of these was a <em>scalar</em> intervention &mdash; the same coupling applied to every region at once, a uniform push or pull on the entire connectome. It worked, and it was honest, but it carried a silent assumption: that a brain state is set by <em>one</em> global parameter, with no spatial address.</p>
<p>That assumption makes three of the most clinically important questions in neuroscience <strong>unaskable</strong>. The first is <strong>focal versus diffuse</strong>: a seizure focus, a stimulating electrode, a stroke &mdash; these are <em>local</em> events, a drive or a fault applied to <em>some</em> regions and not others, and a global scalar cannot represent a profile that is high in one place and zero in another. The second is <strong>off-target spillover</strong>: when a drive <em>is</em> focal, where does its effect actually land &mdash; does it stay where it was applied, or propagate to distant regions through the field? A scalar has no &ldquo;where&rdquo; for the effect to spread from. The third is <strong>region-specific disease</strong>: why does one pathology express in one circuit and another elsewhere, when both ride the same substrate? If the only handle is a single global number, every disorder is the <em>same</em> disorder at a different setting, and region identity carries nothing. This chapter removes the assumption. It gives the drive a <strong>spatial profile</strong> and reads the field <strong>per region</strong>, and in doing so it makes all three questions askable for the first time &mdash; and answers the structural part of each.</p>
<h2>The spatial drive &mdash; a per-node coupling vector and a per-node field</h2>
<p>The generalisation is the smallest possible one, and that is deliberate. The frozen engine&rsquo;s update for region <span class="mono">i</span> is <span class="mono">d&theta;<sub>i</sub> = &omega;<sub>i</sub> + K<sub>glob</sub> &middot; &sum;<sub>j</sub> W&#8320;<sub>ij</sub> sin(&theta;<sub>j</sub> &minus; &theta;<sub>i</sub>)</span>, with a single scalar <span class="mono">K<sub>glob</sub></span> multiplying the field every region feels. E1 replaces that scalar with a <strong>per-node vector</strong> <span class="mono">K<sub>i</sub></span>, one coupling per region, set by a per-node bias <span class="mono">b<sub>i</sub></span> through the <em>same</em> effective-coupling map the schizophrenia, epilepsy and <a href="/mind/26-plasticity-consolidation/">plasticity</a> modules already use: <span class="mono">k(b) = &kappa;/(1&minus;|b|)</span> for an excitatory bias, <span class="mono">&kappa;/(1+|b|)</span> for an inhibitory one, capped at <span class="mono">2&kappa;</span>. When the bias is <em>uniform</em>, the vector collapses to a scalar and the dynamics are <strong>bit-for-bit the frozen engine</strong>. The drive is therefore a pure generalisation: no new equation, no new constant, just the dial made <em>spatial</em> &mdash; a profile that can be high at one region, zero at another, negative at a third.</p>
<p>To <em>read</em> the spatial consequence, the module computes a <strong>per-node local-coherence field</strong>: <span class="mono">c<sub>i</sub> = &lang; &sum;<sub>j</sub> W&#8320;<sub>ij</sub> cos(&theta;<sub>j</sub> &minus; &theta;<sub>i</sub>) &rang;</span>, the field-weighted local order at region <span class="mono">i</span>, averaged over the steady second half of the integration. This is the <em>local</em> counterpart of the global order parameter <span class="mono">R</span> &mdash; where <span class="mono">R</span> reports how coordinated the <em>whole</em> network is, <span class="mono">c<sub>i</sub></span> reports how coordinated each region is with its <em>own</em> field neighbourhood, weighted by the kernel. Crucially the coherence read-out is a <strong>read-only side computation</strong>: it accumulates the cosine alongside the integration but never enters the <span class="mono">sin</span> update, so the &theta; trajectory &mdash; and therefore <span class="mono">R</span> &mdash; is untouched. The form is <strong>forced</strong> [F] (the frozen kernel plus the existing map); the spatial <strong>profile</strong> &mdash; which node is driven, the depth <span class="mono">b&#8320;</span> &mdash; is <span class="mono">[O]</span> and <strong>swept</strong>, and every sign and structure asserted below is required to hold across a depth sweep <span class="mono">b&#8320; &isin; {0.3, 0.5, 0.7, 0.9}</span>. No number is fit to a target; the spatial findings are <em>signs</em>, certified to survive anti-tuning.</p>
<h2>E1.1 &mdash; the field has a fixed spatial structure: local and normalised</h2>
<p>The first result is the foundation the other three stand on: the field a region exerts has a <strong>fixed spatial structure</strong>, and it is exact, not emergent. Two properties of the frozen kernel <span class="mono">W&#8320;</span> are certified to machine precision. The first is <strong>locality</strong>: for <em>every</em> one of the 12 regions, the kernel weight a region places on its partners <strong>decreases monotonically with anatomical distance</strong> &mdash; the nearer the partner, the stronger the coupling, with no exceptions across all 12 rows. This is the <span class="mono">&sim;1/r&sup3;</span> ephaptic law made explicit: the field is <em>spatially graded</em>, and a region&rsquo;s influence is concentrated on its neighbourhood. The second is <strong>normalisation</strong>: every row of the kernel <strong>sums to exactly 1</strong> (maximum deviation <span class="mono">2&times;10<sup>&minus;16</sup></span>, i.e. floating-point zero). Each region distributes a <em>conserved</em> total field influence across its partners &mdash; row-stochastic by construction, which is what preserves the ephaptic locality whenever the kernel is touched.</p>
<p>These are not findings about dynamics; they are findings about <em>geometry</em>, and that is the point. Locality and normalisation are exact properties of the measured anatomical geometry and the frozen kernel form &mdash; <strong>structure, not a fitted result</strong>. They are what license everything spatial that follows: because the field is local, a focal drive <em>has</em> a neighbourhood to act on; because it is normalised, the total influence is conserved as it is shaped. The substrate of &ldquo;focal versus diffuse&rdquo;, &ldquo;off-target spillover&rdquo; and &ldquo;region-specific&rdquo; is this fixed spatial structure &mdash; and none of it could be expressed by a single global scalar, which has no rows, no distances, and no neighbourhoods at all.</p>
<h2>E1.2 &mdash; perturbation reach is heterogeneous with a drive-stable hub</h2>
<p>The second result asks the off-target question directly: drive <em>one</em> region and watch the <em>global</em> order respond. Define the <strong>reach</strong> of a focal drive as <span class="mono">reach(i) = |R &minus; R&#8320;|</span> &mdash; how far the change propagates to the network-wide state when only region <span class="mono">i</span> is pushed. The reach map is <strong>strongly heterogeneous</strong>: across the 12 regions the largest reach exceeds the smallest by <strong>more than a hundredfold</strong> (max/min above 110 at every swept depth, and above 129 from <span class="mono">b&#8320; = 0.5</span> upward). Network <em>position</em> decides how far a focal drive travels: pushing some regions barely moves the global state, while pushing others reorganises it. The naïve picture &mdash; that a focal drive is &ldquo;a small local perturbation&rdquo; &mdash; is false for the high-reach regions; for them a focal drive is a <em>global</em> event.</p>
<p>The decisive part is that the reach map is <strong>not an artefact of how hard the drive is pushed</strong>. The <strong>cerebellum is the rank-1 reach hub at every swept depth</strong> &mdash; the single region whose focal drive moves the global order the most, at <span class="mono">b&#8320; = 0.3, 0.5, 0.7</span> and <span class="mono">0.9</span> alike &mdash; and the full reach <em>ranking</em> is <strong>invariant for moderate-to-high drive</strong> (Spearman correlation <span class="mono">= 1.0</span> between every pair of depths in <span class="mono">{0.5, 0.7, 0.9}</span>). The hub structure is a <strong>connectome invariant</strong>: the spatial map of which regions broadcast and which stay quiet is a fixed property of the geometry, not of the drive amplitude. This is the structural substrate of off-target effects &mdash; it identifies, before any disorder is named, <em>which</em> sites carry a focal drive away to the rest of the brain and which do not. (It is a structural reach map only; it is <strong>not</strong> a claim about a real electrode, a real current spread, or a clinical target &mdash; <span class="mono">efficacy = 0</span>.)</p>
<h2>E1.3 &mdash; the self-localising vs relay classification is drive-invariant</h2>
<p>The third result is the <strong>headline of the layer</strong>, and it sharpens the reach finding from a global magnitude into a per-region <em>class</em>. Drive one region focally and read the <em>full</em> local-coherence footprint &mdash; the vector of changes <span class="mono">&Delta;c<sub>j</sub></span> across all regions. Each driven region falls into one of two classes. A <strong>self-localising</strong> region is one whose <em>own</em> coherence change is larger than the mean change it induces off-target: the footprint <em>concentrates</em> at the driven site. A <strong>relay</strong> region is the opposite: driving it changes <em>distal</em> coordination more than its own &mdash; the drive is <em>relayed</em> to other sites rather than contained. This is the precise structural form of &ldquo;does a focal drive stay focal?&rdquo;: for self-localising sites, yes; for relays, no.</p>
<p>The certification is that this binary classification is <strong>identical across the entire depth sweep</strong>. At every drive amplitude from <span class="mono">b&#8320; = 0.3</span> to <span class="mono">0.9</span>, the <em>same</em> ten regions self-localise and the <em>same</em> two relay: the <strong>relay set is <span class="mono">{hippocampus, midbrain}</span></strong>, fixed at every amplitude tested. Whether a site <strong>contains</strong> its drive or <strong>relays</strong> it is therefore a <em>fixed property of where it sits in the field</em>, not of how hard it is pushed &mdash; a drive-invariant connectome property, exactly as the reach hub is. And it coheres with E1.2: the two relay regions are precisely the kind of sites whose drive lands elsewhere, and the midbrain in particular shows up as a high-reach site at low drive &mdash; a relay broadcasts. This is the structural map of <strong>focal-disease spread versus containment</strong> &mdash; which foci would stay local and which would broadcast, and which sites would carry targeted stimulation <em>away</em> from its intended target. (Axis-A firewall: a self/relay class is a structural spatial property of the coupling model, <strong>never</strong> a claim about the felt locus of an experience.)</p>
<h2>E1.4 &mdash; there is no universal focal&gt;diffuse law: the honest no-tuning result</h2>
<p>The fourth result is the one the no-tuning discipline exists to produce: a clean, attractive hypothesis, tested and <strong>found false</strong>, reported as the finding rather than quietly dropped. The hypothesis is the obvious one a clinician or an engineer would reach for first: <em>focal drive concentrates local gain at its target more than the same dose spread diffusely</em>. Deliver a fixed total dose two ways &mdash; all of it to one region (focal), or the same total split evenly across all regions (diffuse) &mdash; and compare the coherence gain <em>at the focal target</em>. If the hypothesis held, the focal delivery would always win at its own site.</p>
<p>It does not. Across the 12 regions the sign of <span class="mono">(focal target-gain &minus; diffuse target-gain)</span> <strong>varies</strong>: at 9 of the 12 sites focal delivery concentrates more gain at the target, but at the other 3 it does <em>not</em> &mdash; for those sites the same dose spread diffusely produces <em>more</em> local gain at the would-be target than concentrating it there. There is <strong>no universal focal&gt;diffuse law</strong>. We do not force the monotone narrative the heterogeneous-frequency physics refuses to support; the refuted hypothesis is reported honestly, and that honesty <em>is</em> the no-tuning discipline working as intended. The genuine finding is the deeper one: spatial <strong>outcome is site-determined</strong> &mdash; governed by network position (E1.2, E1.3), not by a global rule that holds everywhere. And that has a direct consequence for everything built on this layer: because the spatial consequence of a drive depends on <em>which</em> region receives it, disease modules on this layer must be <strong>region-specific, never global</strong>. The layer does not hand its successors a universal stimulation law; it hands them a <em>map</em>, and the instruction to read it per region.</p>
<h2>S4 &mdash; the engine-invariance guard: a uniform drive recovers the frozen anchor</h2>
<p>As with every layer in the series, the spatial drive is certified to be a <strong>pure add-on</strong>. When the per-node bias is set to <em>zero everywhere</em> &mdash; a uniform drive, the vector collapsed back to the scalar &mdash; the integration reproduces the frozen <strong>M9 coordination anchor</strong> <span class="mono">R = 0.38961455156044245</span> <strong>bit-for-bit</strong>, identical to the engine&rsquo;s own direct value at full numerical precision, and the per-node local-coherence field equals the stored baseline field <em>exactly</em>. Turning the spatial profile off recovers the frozen engine with nothing left over. The engine is emerged <strong>read-only</strong> for the invariant check and confirmed <strong>byte-unchanged</strong> against the frozen tree hash (<span class="mono">0fbf4988fc83&hellip;</span>), with the M0&ndash;16 subtree identical &mdash; the same guarantee the plasticity and state-switching layers carry. The spatial layer adds an axis; it changes nothing that was already there.</p>
<h2>The four results side by side &mdash; what the layer establishes</h2>
<p>The layer is, at heart, four certified statements about a single new object &mdash; the per-node spatial drive and the field it shapes. Read across a row to see one result and the discriminant that carries it; read down to see how the layer moves from <em>fixed structure</em> (geometry) through <em>heterogeneous reach</em> and a <em>drive-invariant class</em> (the spatial map) to the <em>honest absence</em> of a universal law (the no-tuning result).</p>
<table class="triad">
  <thead>
    <tr><th>result</th><th>what it establishes</th><th>the discriminant</th><th>grade</th></tr>
  </thead>
  <tbody>
    <tr>
      <th>E1.1<br>field structure</th>
      <td>the kernel is <strong>local</strong> (weight monotone-decreasing with distance, all 12 rows) and <strong>normalised</strong> (every row sums to 1)</td>
      <td>exact geometry &mdash; deviation <span class="mono">2&times;10<sup>&minus;16</sup></span>, the substrate of every focal/region claim</td>
      <td><span class="mono">[V exact]</span></td>
    </tr>
    <tr>
      <th>E1.2<br>reach map</th>
      <td>perturbation reach <span class="mono">|R&minus;R&#8320;|</span> is <strong>heterogeneous</strong> (max/min <span class="mono">&gt;110</span>) with a <strong>drive-stable hub</strong></td>
      <td>the cerebellum is rank-1 at every depth; ranking invariant for <span class="mono">b&#8320;&ge;0.5</span> (Spearman <span class="mono">=1.0</span>) &mdash; a connectome invariant</td>
      <td><span class="mono">[V mech]</span></td>
    </tr>
    <tr>
      <th>E1.3<br>self / relay <em>(headline)</em></th>
      <td>each node&rsquo;s footprint <strong>self-localises</strong> or <strong>relays</strong>, and the class is <strong>drive-invariant</strong></td>
      <td>the relay set <span class="mono">{hippocampus, midbrain}</span> is fixed at every amplitude &mdash; containment vs spread is a property of position</td>
      <td><span class="mono">[V mech]</span></td>
    </tr>
    <tr>
      <th>E1.4<br>no universal law</th>
      <td>there is <strong>no</strong> universal focal&gt;diffuse law; spatial outcome is <strong>site-determined</strong></td>
      <td>the sign of (focal&minus;diffuse) target-gain varies (9/12 concentrate) &mdash; a refuted clean hypothesis, reported honestly</td>
      <td><span class="mono">[V mech]</span></td>
    </tr>
  </tbody>
</table>
<p>The table makes the layer&rsquo;s logic legible. E1.1 is the <em>fixed structure</em> the field has before anything is driven. E1.2 and E1.3 are the <em>spatial map</em> that structure produces: a heterogeneous reach with a stable hub, and a binary containment/spread class that does not change with drive amplitude. E1.4 is the <em>honest boundary</em>: the map is real, but it does not reduce to a universal law, so it must be read per region. Together they are the spatial axis the global-drive atlas never had &mdash; built once here, handed to every region-specific module that follows.</p>
<h2>What the layer does not claim &mdash; the firewall</h2>
<p>The spatial layer adds an axis to the model, and it must be stated plainly what that axis is <em>not</em>. <strong>Every quantity certified here is a structural spatial quantity</strong> &mdash; a local-coherence field, a reach map, a self/relay class, all of them properties of how a coupling model coordinates across a fixed kernel &mdash; and <strong>none of them is ever a claim about the felt locus of an experience</strong>. That a drive &ldquo;localises&rdquo; at a region is a statement about where coordination changes in the connectome model; it is <em>not</em> a statement that an experience is felt <em>there</em>, or felt at all. This is the Axis-A firewall, held exactly as in every chapter of the series: <span class="mono">consciousness_claim = 0</span>, and the hard problem of experience stays <strong>open</strong>. Giving the drive a spatial address does not give experience one.</p>
<p>The boundary to clinical reality is just as firm. The per-node bias is <strong>not</strong> a real electrode, the local-coherence field is <strong>not</strong> a real current density, and the focal/diffuse dose is <strong>not</strong> a real stimulation protocol. Real volume conduction and ephaptic coupling are <em>heterogeneous</em> and frequency-dependent &mdash; tissue conductivity anisotropy, gyral geometry, myelination &mdash; and this module asserts the <em>sign and structure</em> of a per-node drive on the <em>frozen</em> kernel, <strong>not</strong> that any real cortical field follows this exact <span class="mono">1/r&sup3;</span> law. Every magnitude is <span class="mono">[O]</span>: representative reads over swept profiles, never quantities fitted to a target. E1 is the <strong>layer</strong>, not an application; the region-specific disorders that will use it &mdash; focal epilepsy foci, stroke and lesion fields, off-target effects of targeted neuromodulation &mdash; are <strong>owed to later modules</strong>, and the <span class="mono">SpatialField</span> class is the reusable object they will import. There is <strong>no new mechanism, no new measurement beyond the read-only field, and no new tuned constant</strong> in this chapter; the engine is imported read-only and byte-unchanged. <strong>Nothing</strong> here is a cure, a reversal, a prevention, a treatment, a recommendation, or a dose. <span class="mono">efficacy = 0</span>; this is not medical advice, not a diagnosis, and not a treatment protocol. What the layer offers is exactly one thing, and it is a structural statement: the field has a <em>fixed</em> spatial structure, a focal drive&rsquo;s reach and containment are <em>fixed properties of position</em>, and there is <em>no</em> universal focal-versus-diffuse law &mdash; so the spatial story of disease must be told region by region, never with a single global dial.</p>
<nav class="pn">
  <a rel="prev" href="/mind/42-e0-triad-synthesis/">&larr; §42 The E0 triad synthesis</a>
  <a href="/mind/">paper contents</a>
  <a rel="next" href="/mind/44-focal-epilepsy-spread/">§44 Focal epilepsy: containment vs secondary generalisation &rarr;</a>
</nav>
</main>
<footer>DOI <a href="https://doi.org/10.5281/zenodo.20694404">10.5281/zenodo.20694404</a> · ORCID <a href="https://orcid.org/0009-0002-7535-8245">0009-0002-7535-8245</a> · <a href="https://creativecommons.org/licenses/by/4.0/">CC BY 4.0</a></footer>
</body>
</html>
"""


def main():
    outdir = os.path.join(MIND, SLUG)
    os.makedirs(outdir, exist_ok=True)
    path = os.path.join(outdir, "index.html")
    with open(path, "w", encoding="utf-8") as f:
        f.write(HTML)
    print(f"wrote {path} ({len(HTML)} bytes)")


if __name__ == "__main__":
    main()
