# HANDOVER — main-carrier M16-study (add-only; frozen engine untouched)

**Session goal (given):** drop the philosophical frame; in physics/observation/evidence/causality
only, **track the access-correlated high-frequency wave**, **find its source and emerge it**, set a
**strong pre-registered hypothesis + verification**, and go deep on **parallel carriers → memory**.

**Done, frozen, add-only.** The locked engine `vp_mind_engine.py` and its tree hash
`b18c8626…` were **not touched** (no byte changed; the 112-check regression stands). The new work
is a standalone study that **imports the engine read-only**:

- `repro/mind/_consciousness/vp_main_carrier_emergence.py` — stages A–E + self-check gate.
- `repro/mind/_consciousness/verify_main_carrier.py` — re-emerges and checks hash + invariants + gate.
- `repro/mind/_consciousness/main_carrier_results.json` — frozen rounded results.
- `repro/mind/_consciousness/expected_main_carrier_sha256.json` — frozen hash + 11 invariants.
- `repro/mind/_consciousness/MAIN_CARRIER_emergence.md` — the study note (grades + references).

**Frozen headline sha256:** `8d05cfeccb9cf57c924b1b2aea3f716caaf6069f690171bf8d2fae47004b2de7`
(SEED=19, 2× bit-identical). **Gate 13/13**, **11 invariants**, **0 new tuned constants**.

## What the study established (all reusing measured/derived constants only)

1. **Source emerged (the "근원").** The carrier's frequency is set by the **measured GABA_A decay
   τ = 6.0 ms** (Destexhe 1998; inhibition-decay→gamma, Bartos 2007), generated by **PV fast-spiking
   interneurons** (Cardin 2009 / Sohal 2009, causal). Emergent carrier > slow band, **monotonic 1/τ**,
   activity range [0.034, 0.0768] (engine units; **absolute Hz stays [O]**, ratio 6.125 is the invariant).
2. **State selectivity [V/I].** A **structured fast** carrier writes a recoverable memory (recall 1.000);
   a **large UNSTRUCTURED slow** drive (up to 60×, deep-SWS analogue) collapses to chance (0.700). The
   operative carrier is **wake/REM high-frequency**, not **large-amplitude deep-SWS** — concordant with PCI.
3. **Metastable window [V].** Inverted-U: silence 0.675 | metastable 1.000 | global-sync (seizure) 0.692.
   The biggest/most-synchronised field is NOT operative (M9 R<0.9 window, memory side).
4. **Parallel multiplex [V/I].** capacity floor(6.125)=**6 slots** (7±2, Lisman); six contents reinstated
   from the **slow index alone** at fidelity 1.000 with **cross-talk 0.497 (chance)** → specific,
   content-addressable; integrated parallel drive crosses the R19 fold 0.385.
5. **Honesty carried.** `medium_efficacy_tested=0`, `consciousness_claim=0`, `hard_problem_open=1`,
   **PCI access marker = HONEST NEGATIVE** (not reproduced). No subjective-experience claim.

## Reproduce / verify

```bash
cd repro/mind/_consciousness
python3 vp_main_carrier_emergence.py   # 13/13 gate, headline 8d05cfec…
python3 verify_main_carrier.py         # VERIFY PASS (hash + 11 invariants + gate)
```

## Next entry points (honest OPEN)

- **Promote to a governed module M16 `emerge_main_carrier()`** + chapter §17, locking these
  invariants under the existing gate battery (built exactly as M11/M14 were; engine hash would then
  change intentionally with cascade re-check + history preserved).
- **Ground the SLOW (theta) index's pacing** the way the carrier is now grounded: its absolute pacing
  is still [O]; a measured septal/Ih anchor would upgrade the ratio's slow leg.
- **Tie to in-vivo efficacy.** When a field-cancel-vs-augment two-electrode record (neuro §9/§19) or a
  PCI-style perturbation dataset arrives, test whether biology *uses* the carrier; until then
  `medium_efficacy_tested=0`.
- **Anti-p-hacking note for any extension:** the carrier τ, the unstructured-amplitude sweep, the
  parcellation, and the capacity are fixed BEFORE reading results; report robustness, never pick to hit
  a target. grade==evidence.
