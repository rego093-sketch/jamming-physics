# repro/mind/07-selection-loop

REPRODUCES OFFLINE (model demo). `inputs/closed_loop_vortex_results.json` — single winner (nsel=1, commit=1; control nsel=0). The basal-ganglia selection circuit ITSELF is cited from the neuro paper (direction); only the eddy-selection demo is here. Absolute ms/dopamine OPEN.

Offline regression for the bundled artifacts: `../_verify/run_regression.py` (REGRESSION PASS, mechanism only, seed=7, bit-for-bit). This paper is a FRONTIER paper: the regression demonstrates the mechanism behaves as claimed; it does NOT confirm the functional theory of consciousness, and there is no validated marker of consciousness (PCI = honest negative). Component circuits inherit their direction support from the neuro paper; the dependency is one-way.
