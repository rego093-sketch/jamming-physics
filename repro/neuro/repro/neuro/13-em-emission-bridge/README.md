# §13 — EM emission, the antenna logic (physics bridge)

`vp_em_emission.py` verifies that a **shaken rotor** (an accelerating charge = a rotating
defect exerting a time-varying force density f) **emits a real transverse wave** that
propagates at c and carries energy outward — the *sourced* Maxwell of physics §14.0.6b.
Curl form forced by isotropy; Faraday/charge-conservation forced; only the radiation
EFFICIENCY (the antenna problem) is open `[O]/[H]` (the coupling αₑₘ is a measured input).

Run: `python3 vp_em_emission.py` (deterministic; 1-D + 2-D radiation, 2× sha256 identical).

**Guardrail (neuro §9, retired):** this is the PHYSICS emission logic. The brain does
**not** use emitted EM as a signalling or consciousness carrier — "EEG as an EM carrier"
is physically falsified; neural conduction stays ionic (0.5–120 m/s, ~10⁶× slower than
light). This bridge is kept here only so the physical boundary is explicit and verified.
