# WORK_HANDOVER — next session (after v1.11.0 — ANALGESIC INHERITANCE + neuro-native pain + explicit DNA-emergence)

> **v1.11.0 (this session) — inherited the analgesic technique into its home volume.** The sibling
> `analgesic_threshold_logic` v2.0 (DOI 10.5281/zenodo.20733420) was itself built on THIS engine
> (`repro/neuro/_engine/vp_neuro_engine.py` is byte-identical, sha256 `c933ee8d…`), so the nociceptor
> firing threshold its 27-target map raises IS this volume's own |h_sp| = spinodal(γ). Three new
> **separate** chapters were added (VP-SPEC v1.8 §6): **§21** the inherited 27-target three-lever map
> re-derived here (**drift 0**), **§22** neuropathic pain as a firing-threshold shift + the three
> improvement levers (this volume's native acquired/dynamics-key disease; monogenic channelopathies
> cross-referenced to disease_wp), **§23** the channelopathy direction anchor + the explicit
> DNA-emergence grounding (`inherited/organ_gamma.json`; form ← γ). Added the **C4 retrieval files**
> that were missing in v1.10.1 (`docs/assets/css/site.css`, `robots.txt`, `sitemap.xml`,
> `llms.txt` < 5 KB) and `faq/` + `methods/` pages. Extended the hub TOC to **all 24 chapters**
> (orphans **0**). New gate `tools/gate_neuro_v1_11.py` (**11/11**), auto-collected by
> `verify_all.py` → **PASS (6/6)**; `run_all.py` now **21 modules**, all deterministic + frozen.
> **Firewall kept verbatim:** γ reads STRUCTURE only (voltage/potency/dose/selectivity/effect all
> **[O]**); felt pain is the **mind** volume's; δ is structural, not a dose; nothing prescribes.
>
> **Next-session candidates (all optional, none blocking):**
> 1. Metadata-only JSON-LD upgrade of §0–§20 to the richer v1.8 `CreativeWorkSeries` form.
> 2. Apply the §23 DNA-emergence route forward to the remaining master-gene chapters.
> 3. On publication, mint the v1.11 release DOI and wire it into the new chapters + registry.
> 4. Live-wire the digestive→neuro pointer and the disease_wp gene-key cross-reference (currently
>    declared contracts only, VP_FRAMEWORK_MAP §9.2).

---

# WORK_HANDOVER — next session (after v1.10.1 — §03 γ/θ RATIO IN-PACKAGE; builds on v1.10.0 sensory atlas)

> **v1.10.1 (this session) — §03 dimensionless γ/θ ratio brought IN-PACKAGE.** Closes the **last
> actionable in-track item** (open item #5 below): the §03 working-memory ratio γ/θ ≈ 7±2 was
> previously reproducible **only** in the separate `neuro_extension` lane. This session adds a
> **minimal in-package gate** `repro/neuro/03-rhythm-bands-coupling/verify_band_ratio.py` so the
> **dimensionless ratio — and only that claim** — re-checks from the one zip alone (**BAND-RATIO LOCK
> PASS 5/5**, stdlib-only, bit-for-bit). The ratio is **derived** as the ratio of geometric-mean band
> centres (`√((25·50)/(4·8)) = 6.25`, the package's own non-tuned construction, as in §14) and
> **validated by containment** in Miller's **independent** 7±2 = [5,9]; the check is falsifiable
> (broad gamma 30–100 Hz → 9.68, outside the window, so it **selects** the theta-coupled slow gamma
> per Colgin 2009). Inputs are locked + cited in a self-verifying JSON (`payload_sha256`, tamper-tested);
> absolute Hz stay `[O]` (graded in the new `…/03-…/IRREPRODUCIBILITY_LEDGER.md`). The gate is kept
> **outside** `verify_all.py` (like `verify_em_thesis.py` / `verify_boundary.py` / `verify_terminology.py`),
> so **`verify_all.py` stays 5/5** and **`run_all.py` stays 18 modules, all hashes byte-identical** (no
> existing module touched). The full §03 model+real-data regression stays in `neuro_extension`,
> unchanged — purely additive. No constant tuned (C1 holds). See `CHANGELOG_v1_9.md` **v1.10.1** (top).
> DOI of record unchanged (10.5281/zenodo.17979015); spec_version stays **1.8**.
> **Anti-drift rule for §03:** the in-package gate covers ONLY the dimensionless ratio; the ~17×
> theta-phase modulation and the τ_inh sweep / disease directions remain the disclosed separate lane —
> do not claim they are in-package. Run `verify_band_ratio.py` after any §03 band-edge edit.

> **v1.10.0 (prior session) — SENSORY ATLAS COMPLETE.** The milestone that **closes the sensory/neural
> side** of the chain. The motor/EM side was already done (§13 emission · §14 motor · §15 full EM link ·
> §16 force · §17 spinal CPG · §18 brain-circulation field · §19 ephaptic threshold). The open gap was
> sensory: §10/§12 had emerged five modalities (vision · hearing · smell · taste · skin-temperature),
> leaving touch, pain, proprioception, and balance. This session adds the **§20 capstone** in which the
> four remaining modalities **emerge from real measured γ** (14 master genes fetched from NCBI and cached
> for offline reproduction; corr(γ,GC)=0.997) and transduce — so all **nine** human sensory modalities now
> stand on the same R19-switch substrate. The somatosensory triad (touch/warmth/**pain**) lives on the one
> emerged skin organ with the high-vs-low threshold ORDER anchored to the **measured TRPV1 43 °C** (Caterina
> 1997); proprioception (RUNX3 · PIEZO2 spindle) **closes the §11 reflex loop** onto an actual afferent
> (rejection ×10.4); the vestibular hair cell (ATOH1 · OTOP1) is **directional/signed**. The engine was
> extended **additively** (2 stimuli + 3 receptors), so all 17 prior modules stay **byte-identical**.
> `verify_all.py` → **5/5**; `run_all.py` → **18 modules**, deterministic, hashes frozen, drift 0. No
> constant tuned to a target (C1 holds); every illustrative magnitude is graded `[O]` in the new
> `repro/neuro/20-complete-sensory-atlas/IRREPRODUCIBILITY_LEDGER.md`. See `CHANGELOG_v1_9.md`
> **v1.10.0** (top). DOI of record unchanged (10.5281/zenodo.17979015); spec_version stays **1.8**.
> **Near-completion status:** the sensory side is now closed the same way the motor/EM side was; what
> remains genuinely open is **subjective experience**, deferred by design to the companion `mind` paper —
> the declared boundary, not a gap in the emergence chain.

> **v1.9.4 — EM near/far thesis LOCK.** Builds directly on v1.9.3 §19. The §9 bounds
> register's **answer/abstract** listed "EM" flatly among the retired items, contradicting §18/§19
> and §9's own amendment — the single source of the recurring "retire EM" drift. v1.9.4 fixes that
> headline (→ "radiative EM **far-field** carrier", with the **near-field affirmed**), updates the §9
> amendment ("weak" = **scalp** EEG only; **local** near-field **at threshold** per §19), and adds the
> canonical **`EM_NEAR_FAR_THESIS.md`** (root) plus a standalone gate
> **`repro/neuro/09-bounds-open-retired/verify_em_thesis.py`** (**6/6 PASS**, both boundaries pinned;
> outside `run_all`, so `verify_all.py` stays **5/5**). See `CHANGELOG_v1_9.md` §0e. **Anti-drift rule:**
> "EM" is never a blanket retired item — only the radiative far-field/TIR carrier is; run the gate
> after any EM-touching edit.

This package is `neuro_emergence_chain_integrated` at **v1.10.0**. `python3 verify_all.py` →
**OVERALL: PASS (5/5)**. v1.9 removed the tuning that v1.8 carried and bound the §14 middle
link to measured biology; the **v1.9.1** point release closed the last v1.9 open item by locking §14
recruitment to two *cited* motoneuron datasets (`CHANGELOG_v1_9.md` §0). A prior session added the
**§18 capstone** — "EM near-field circulation in the brain" — which **wires the package's own
modules together** (the engine's FHN oscillators + §13/§15 lattice emission + the inherited
light anchor) into one closed near-field circulation loop, builds the *physics* of that
circulation (`[V]`), and keeps the *functional* question OPEN and deferred to Mind
(`CHANGELOG_v1_9.md` §0b). The **v1.9.2** point release (this session) then closed the two remaining
§14 items: the force-frequency saturation is now **derived from twitch fusion** (cited
Fuglevand-Winter-Patla 1993; the dimensionless fusion index in ν=f·T rises monotonically and
saturates, reproducing Rack & Westbury 1969), and the dual-code illustration is unified onto the
**measured rheobase pool** (the toy 20-unit `Muscle` removed from §14; the engine and §11 untouched)
(`CHANGELOG_v1_9.md` §0c). The **v1.9.3** point release (this session) advanced the **EM thread** per author direction — EM is *not* retired by bias — by adding the **§19 ephaptic-threshold** capstone: it corrects §18's one un-measured word (*"weak"*, which conflated the µV **scalp** EEG with the mV/mm **local** near-field) by **quantifying** the local endogenous near-field against the **measured** ephaptic threshold. Scaling the measured cortical field (Fröhlich & McCormick 2010, |avg| 2.29 mV/mm) by the measured sensitivity (Bikson 2004, 0.12 mV per mV/mm) gives a derived `ΔVm = 0.2748 mV` **contained** in the independently-measured bound (Anastassiou 2011, `< 0.5 mV`) — the field sits **at** the threshold (ratio ≈ 1), **not** below it `[V]`; the far-field radiative carrier stays **retired** (near-field ephaptic ≠ far-field broadcast) and the *functional* question stays **OPEN → Mind**, with a named decisive experiment (`CHANGELOG_v1_9.md` §0d). The governing rule is unchanged and absolute:

> **Every constant is either a measured input (locked + cited) or a derived value — never a
> number chosen to hit a target.** Each open item names its obstacle in a ledger. Verify with
> the relevant gate after **every** change. Keep the deliverable as **ONE** package zip.

## ► How to hand this off — ONE file (read this first)

**To continue THIS (neuro) track next session, re-upload exactly one file: the package zip
`neuro_emergence_chain_integrated_v1_10_0.zip`.** Everything needed is inside it and nothing else
has to be brought back:

- `VP_SPEC_v1_8.md` is **bundled at the package root** — do not re-upload it separately.
- All engines, inherited modules, the reproduction harness, the gates, every chapter's HTML, the
  frozen hashes, `_meta.json`, `CHANGELOG_v1_9.md`, and this handover are inside the zip.
- First action on receiving it: extract, `cd` in, run `python3 verify_all.py`, confirm **5/5**.
  That single command re-establishes the entire trusted state from the one file.

**The other tracks are their own separate one-file deliverables — keep them apart, do not merge:**

- **Mind / Felt Cognition site** (`mind_vp_site_UPGRADED…zip`) — the companion HTML whitepaper, an
  *upgrade in progress*. This is where §15/§18's deferred OPEN question ("does the brain *use* the
  near-field / ephaptic coupling for coordination?") is meant to be taken up. Not touched this
  session. Hand off as its own zip.
- **DNA-interpretation track** (`neuro_emergence_chain_integrated_v1_9.zip`) — the near-identical
  earlier point release the author uses as the "DNA 해석법" line (differs only at §14 motoneuron-dataset
  locking). Its own zip; do not fold into v1.9.1.

So: **one file per track.** For the neuro chain, that one file is the v1.9.4 zip below.

**The neuro⟷mind boundary is now written down and gated (added this session).** The full
two-sided definition — ownership (IN/OUT), the one-way dependency (mind cites neuro; neuro never
depends on mind), the lane/one-file-per-track rule, the exact OPEN items that cross to Mind, and
the single shared `EM_NEAR_FAR_THESIS.md` — lives in **`PROJECT_BOUNDARY_neuro_mind.md`** (package
root). Its neuro-side invariants are mechanically enforced by **`verify_boundary.py`** (root,
**5/5 PASS**, report `reports/lock-neuro-boundary.gate.json`): no neuro→mind code import · lane
purity · shared SSOT present · EM lock holds · standalone contract intact. Kept outside `run_all`,
so `verify_all.py` stays **5/5**. When the **Mind** track is next opened, mirror **three** root
files byte-identical into the mind package — `PROJECT_BOUNDARY_neuro_mind.md`, the **refined**
`EM_NEAR_FAR_THESIS.md` (now with the §1.1 near-field A/B disambiguation, v1.9.5), and the new
`TERMINOLOGY_canonical.md` (the controlled vocabulary, which governs mind §2/§4 too) — and run the
mind-side checklist in `PROJECT_BOUNDARY` §8. *(v1.9.5 also added `verify_terminology.py` at the
root — the regression guard that keeps the "near-field" overload from returning; run it alongside
`verify_boundary.py`.)* *(Also this session: the two shipped `__pycache__/`
build-artifact folders were removed — they are regenerated on run and should not ship; no source,
hash, or gate affected.)*

## What v1.9 did (see `CHANGELOG_v1_9.md` for the full record)
- **§16** plateau top reported as the derived **range 2.20–2.25 µm**, GHJ 2.20 validated by
  **containment** (lower edge), not an exact `|Δ|=0` fit.
- **§17** ventral→dorsal order now **derived** from measured adjacency + threshold ranking
  (read from data), not hard-coded; output bit-identical.
- **§14** recruitment order **derived from Ohm's law** (rheobase = dVth/R_input); v1.9.1 locks the
  inputs to two **cited** cat-motoneuron datasets (Fleshman 1981; Gustafsson & Pinter 1984), validates
  the derived rheobases by **containment** in the measured range, and reports the measured span-excess
  (threshold rises with rheobase) as `[O]`; the circular twitch→tetanus assertion removed (ratio =
  locked measured input). Absolute newton scale `[O]`. **v1.9.2** then **derives** the saturating
  force-frequency from **twitch fusion** (Fuglevand-Winter-Patla 1993; dimensionless fusion index in
  ν=f·T rises and saturates, reproducing Rack & Westbury 1969) and unifies the dual code onto the
  measured rheobase pool (toy `Muscle` removed from §14); absolute fusion rate + mean-force plateau `[O]`.
- **§03** dimensionless γ/θ ≈ **7±2** made explicit as the one validated quantity; absolute Hz `[O]`.
- **Tooling:** builder writes to package-root `docs/` (was `tools/docs/`); `_meta.json` §14
  one-liner corrected to the honest result. (`spec_version` stays **1.8** — the VP-SPEC version
  the package conforms to, matching the gate reports and `tools/package_neuro_v18.py`; an earlier
  hand-bump to "1.9" was reverted in the v1.9.1 consistency audit, since no VP-SPEC v1.9 exists and
  the package version v1.9.1 is tracked separately in the CHANGELOG.)
- **§18 capstone (this session):** new chapter that *connects* existing modules rather than
  re-deriving — engine `Neuron` (FHN) oscillators on a ring, §13/§15 lattice emission, light anchor.
  Shows (a) every EEG band deep in the near field (`r/(λ/2π)≈10⁻⁸`, radiated fraction ≈10⁻¹⁶), so EM
  is not the timing; (b) the field circulating in **lockstep** with the ionic wave (winding 1, measured
  0.98225) at v≈3.20 m/s ≈ conduction speed — clock is **ionic**; (c) δ/θ/α/β/γ multiplexed at ×-talk
  ≈5×10⁻¹⁶. Physics `[V]`; **function OPEN → Mind**; retired claims (light-speed axon, radiative EEG
  carrier) stay retired; no tuned constant (geometry/conduction inputs marked `[O]`). New files:
  `repro/neuro/18-em-brain-circulation/vp_em_brain_circulation.py`, `docs/neuro/18-em-brain-circulation/index.html`;
  wired into `run_all.py` (MODULES + NEW_CHAPTERS), hash frozen, `_meta.json` → 19 chapters / 9453 words.
- **§19 capstone (this session, v1.9.3):** new chapter `19-em-ephaptic-threshold` that **quantifies**
  §18's local near-field against the **measured** ephaptic threshold — the author's directive to
  advance EM, not retire it by bias. It **imports §18's near-field** (`near_field_geometry`,
  `ring_positions`, `near_field_locality`, `R_BRAIN`, `BANDS`, `N_REG`), so the carrier is literally
  §18's field, not a new one. (a) Confirms the local 1/r³ near-field (NN share 0.3976) with the
  far-field carrier staying retired (radiated fraction 1.143×10⁻¹⁶, lag 3.402×10⁻⁹ cyc); (b) supplies
  the §15/§18 absolute αₑₘ as the measured field `|avg| 2.29 mV/mm` [Fröhlich & McCormick 2010] `[O]`;
  (c) **derives** `ΔVm = s·E = 0.12 × 2.29 = 0.2748 mV` [Bikson 2004] and **validates by containment**
  inside the independently-measured `< 0.5 mV` [Anastassiou 2011], margin 0.2252 `[V]`; (d) shows the
  field is **at** the entrainment threshold (ratio ≈ 1, not ≪1) and entrains spike timing `[V]`.
  Verdict: physics `[V]`; **function OPEN → Mind** (the in-vivo behavioural role is untested — *not* the
  field being weak), decisive test named (behaviour-labelled intracranial field **cancel-vs-augment**);
  retired far-field claims stay retired (near-field ephaptic ≠ far-field broadcast). New files:
  `repro/neuro/19-em-ephaptic-threshold/vp_em_ephaptic_threshold.py`,
  `repro/neuro/19-em-ephaptic-threshold/inputs/ephaptic_field_properties.json` (self-verifying
  `payload_sha256`, 4 cited papers), `docs/neuro/19-em-ephaptic-threshold/index.html`; wired into
  `run_all.py` (MODULES + NEW_CHAPTERS), hash frozen, `_meta.json` → **20 chapters / 10451 words**.
  **§18 prose** got a one-clause correction (scalp-EEG vs local near-field) + `next`→§19 link, with
  **no number changed**, so the §18 module and its frozen hash are untouched.

## Remaining open items (honest — do not paper over)

1. **✅ DONE in v1.9.1 — §14 recruitment is now bound to CITED motoneuron datasets.** The
   representative threshold/resistance constants are replaced by two locked, cited, verbatim-checked
   datasets in `inputs/motoneuron_properties.json` (self-verifying `payload_sha256`): **Fleshman et
   al. 1981** (cat MG: input resistance 0.8–5.1 MΩ, rheobase 0.8–17.1 nA) and **Gustafsson & Pinter
   1984** (rheobase ∝ conductance, range exceeds conductance range ~2×). A single derived 7.471 mV
   threshold (geometric-mean cell, not tuned) puts the pure-Ohm rheobases at 1.465→9.339 nA, validated
   by **containment** in Fleshman's measured range (§16-style; the engine asserts it). The measured
   rheobase span (×21.375) exceeds the conductance-only prediction (×6.375) by ×3.353 — the G&P
   threshold-drift signature, kept as `[O]`. No constant is tuned. The validated claims are the
   recruitment **order** and the **containment**; the threshold-vs-rheobase drift and absolute newton
   scale remain `[O]`. *(If a future session sources a single dataset reporting paired per-cell
   resistance+rheobase, a per-cell |Δ| check could be added — but the containment binding is honest
   and complete as-is.)*

2. **✅ DONE in v1.9.2 — §14 force-frequency saturation is now DERIVED (twitch fusion), not linear.**
   The old `Muscle.force()` rose *linearly* with rate; the saturating force-frequency relation is now
   derived with **no free parameter** as **twitch fusion**. A new locked input
   `inputs/muscle_twitch_properties.json` (self-verifying `payload_sha256`) carries the cited
   **Fuglevand-Winter-Patla 1993** analytical twitch `h(τ)=τ·exp(1−τ)` (impulse response of a
   critically-damped 2nd-order system) and the **Rack & Westbury 1969** measured sigmoid. Summing the
   twitch train gives a **fusion index** `FI=F_min/F_max` that rises monotonically 0→1 and **saturates**,
   universal in the dimensionless rate **ν=f·T** (FI = 0.001, 0.622, 0.884, 0.969, 0.998 at ν = 0.1,
   0.5, 1, 2, 8; crosses the *stated* well-fused criterion 0.90 at ν\*=1.083). The module asserts
   monotonicity + saturation; the HTML reports it (drift 0). **Honest scope kept `[O]`:** under linear
   summation only the *fusion* saturates — the **mean-force sigmoid plateau** needs nonlinear calcium
   summation (not modelled), the **absolute** fusion rate `f=ν/T` (T muscle-dependent) is open, and the
   tetanus **amplitude** ~3.9× remains a locked measured input. The `Muscle`-shared-with-§11 cascade
   risk was avoided entirely by **not** editing the engine: the fusion curve is a **§14-local**
   numpy derivation, so `run_loop.py` re-verifies byte-identical. *(If a future session sources a
   measured nonlinear-summation dataset, the mean-force plateau could be derived too — but the
   dimensionless fusion saturation is honest and complete as-is.)*

3. **✅ DONE in v1.9.2 — §14 dual code unified onto the measured rheobase pool.** The dual-code
   crossover (§14 section 3) is now computed on the **same** 5-unit measured rheobase pool used for
   recruitment (2 units @max-rate = 1.980 < 5 units @low-rate = 4.802), not the toy 20-unit `Muscle`.
   The toy `Muscle` is now **entirely removed from §14** (dropped from the import); §14's quantitative
   claims rest only on the cited measured pool + the derived twitch fusion. The engine's `Muscle`
   class is untouched, so §11 (which still uses it) re-verifies byte-identical.

4. **✅ DONE in v1.9.3 — §18's "weak" boundary word corrected by §19 measurement; the EM-function
   question is now the SHARP live OPEN item (→ Mind), with a named decidable test.** §18 had said the
   brain's incidental field is "weak"; §19 shows that is true only of the **scalp** EEG (µV,
   volume-conducted), while the **local** near-field is at the ephaptic threshold — the derived
   `ΔVm = 0.2748 mV` is contained in the measured `< 0.5 mV` and the field/threshold ratio is ≈ 1.
   So the EM near-field is **not** dismissed; the bias is removed by quantification, and the far-field
   *radiative* carrier stays correctly retired (a different object). **What remains genuinely OPEN:
   does cognition functionally USE this measured spike-timing entrainment?** This is open because the
   *in-vivo behavioural role* is untested, **not** because the field is weak. The decisive experiment
   is named in §9/§19: a behaviour-labelled intracranial recording with the **local endogenous field
   cancelled vs. augmented** in real time (Fröhlich ran the in-vitro feedback version). *Next step:*
   this OPEN→Mind question is the natural pickup for the **Mind / Felt-Cognition** track (its own zip,
   below) — do **not** fold it into the neuro chain; the neuro chain's job (build the physics, bound
   it against measurement, name the test) is complete and honest as-is.

5. **✅ DONE in v1.10.1 — the dimensionless §03 γ/θ ≈ 7±2 claim is now reproducible in-package.**
   The minimal offline gate `repro/neuro/03-rhythm-bands-coupling/verify_band_ratio.py` derives
   γ/θ = geomean(25,50)/geomean(4,8) = √39.0625 = **6.25** from four locked, cited band edges
   (self-verifying `payload_sha256`) and validates by **containment** in Miller 7±2 = [5,9]
   (independent psychophysical bound). Falsifiable (broad gamma 30–100 → 9.68, outside the window),
   deterministic (bit-for-bit), and kept outside `verify_all` so it stays 5/5. The **full**
   model+real-data §03 regression (τ_inh sweep, autism/Alzheimer tilts, 17× modulation, OpenNeuro
   ds004117 / ds004752) **remains** the disclosed separate `neuro_extension` lane — only the
   dimensionless headline ratio was brought in-package. **Honest scope `[O]` kept:** absolute theta
   and gamma Hz are not derived (external τ_inh calibration); the 17× phase modulation is the
   separate-lane quantity. All graded in `…/03-…/IRREPRODUCIBILITY_LEDGER.md`. *(If a future session
   wants the model+real-data direction in-package too, port the `neuro_extension` `_verify` with its
   numpy/scipy + OpenNeuro dependencies declared — a larger lane decision, not an open defect.)*

6. **Properly-declared `[O]`s that are correct as-is** (no action needed unless a new anchor
   appears): §16 `zero_short ≈ 1.27 µm` (filament crumpling), ascending-limb tension, absolute
   tension scale, filament dimensions from ruler-protein sequence; §17 absolute Shh profile and the
   absolute threshold values (representative); the absolute Hz of §03/§04.

## Process reminders for the next session
- Run `python3 verify_all.py` after every change; it must stay **5/5**.
- §14's module is BOTH `run_all` module #6 AND in `NEW_CHAPTERS`: any output change needs
  (a) re-freezing its hash in `repro/neuro/expected_sha256.json` (delete the key, run `run_all`
  once to record the correct **full-stdout** hash, run again to confirm "matches frozen"), and
  (b) keeping every ≥3-decimal number shown in `docs/neuro/14-…/index.html` reproduced by the
  module's stdout (rounding-aware).
- **§18 has the same dual role** (it is BOTH a `run_all` MODULES entry AND in `NEW_CHAPTERS`): if you
  edit `vp_em_brain_circulation.py` output, re-freeze its hash the same way (delete its key in
  `expected_sha256.json`, run `run_all` twice) and keep every ≥3-decimal number in
  `docs/neuro/18-em-brain-circulation/index.html` printed by the module. §18 imports the engine
  (`_engine/vp_neuro_engine.py`); changing that engine cascades to §18 too — re-verify after any engine edit.
- **§19 has the SAME dual role AND imports §18.** `vp_em_ephaptic_threshold.py` is BOTH a `run_all`
  MODULES entry AND in `NEW_CHAPTERS`, so editing its output needs the same re-freeze (delete its key
  in `expected_sha256.json`, run `run_all` twice) and every ≥3-decimal number in
  `docs/neuro/19-em-ephaptic-threshold/index.html` must stay printed by the module (16/16, drift 0,
  rounding-aware). **Critically, §19 imports the §18 module** (`near_field_geometry`, `ring_positions`,
  `near_field_locality`, `R_BRAIN`, `BANDS`, `N_REG`) — so **any change to §18's output cascades to
  §19**: after editing §18 (or the engine §18 depends on), re-run §19 and re-freeze it too. §19's
  cited DOIs live in `inputs/ephaptic_field_properties.json` (self-verifying `payload_sha256`) and are
  **not** in the HTML body — keep them out of the body (matching §18) or the HTML↔code numeric check
  and the no-DOI-in-body convention both notice.
- §16/§17 are isolated from `run_all`; they are gated by their slug `run.py` + `gate_neuro_*`.
  Changing a §17 engine **reason string** (a string leaf) requires regenerating
  `repro/neuro/17-…/expected/spinal_cpg_results.json`; v1.9 avoided that by keeping output identical.
- **§03's `verify_band_ratio.py` is a standalone gate** (v1.10.1) kept **outside** `verify_all` — run it
  by hand alongside `verify_boundary.py` / `verify_terminology.py` / `verify_em_thesis.py`. It is
  deterministic (its stdout self-hashes; 2× run → identical sha256), so any edit to it or to
  `inputs/band_ratio_properties.json` must keep that property; if you change a band edge, **re-compute
  the JSON's `payload_sha256`** (sha256 over `json.dumps(measured, sort_keys=True, separators=(',',':'))`)
  or the gate's integrity check trips. The gate covers ONLY the dimensionless ratio; do not extend it to
  claim the absolute Hz (those are `[O]`) or the 17× modulation (separate lane).
- §03 and the chapter prose for §00–13 have **no content JSON** — edit their HTML by hand. §16/§17
  build from `content/neuro-{16,17}-*.json` via `tools/build_chapter_neuro.py` (now writes to `docs/`).
- Do **not** introduce new tuned constants; do **not** re-fragment into per-chapter zips. If a
  change can't be made honestly/verifiably, document it here as remaining-open rather than fudge.
- **Inventory note (honest):** the live chapter inventory is `docs/neuro/_meta.json` (the gates read
  it; now 20 chapters / 10451 words incl. §18+§19). `manifest/neuro.csv` is a **stale v1.8-era inventory**
  that stops at §15 (it omits even §16/§17) and uses an older word metric; **no active gate reads it**,
  and `verify_all.py` is 5/5 without it. It was already stale in the shipped package, so it is left
  untouched rather than half-rewritten — treat `_meta.json` as authoritative. (Same vintage:
  `tools/package_neuro_v18.py`, a v1.8 assembler whose hard-coded chapter list ends at §15 — **do not
  run it**; it would overwrite `_meta` word counts with the old metric. Tools are not rewritten per the
  constitution.)
