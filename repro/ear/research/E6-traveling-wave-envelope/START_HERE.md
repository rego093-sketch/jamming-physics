# START HERE — increment E6 (the traveling-wave ENVELOPE: FORM forced, sharpness Q the [O])

**One-line goal.** Take up the seed's **deepest [O]** — the fluid-loaded dispersive traveling-wave
**ENVELOPE** that the inherited wave module calls *"the named [O] this seed exists to take up"* — and,
without tuning, turn it from a black box into a **characterised** one: prove the envelope FORM is **forced**
by the frozen substrate and that exactly **one** scalar (the sharpness Q) remains open.

## Why this increment exists
E1 forced the traveling-wave **peak place** (inverse-Greenwood) but left the envelope **[O]**; E3 forced the
active **cube root**; E5 forced the **readout**. The envelope itself — the *shape* of excitation around the
place, its asymmetry, its sharpness, its delay — was the original named obstacle the seed was built to push.
E6 pushes it as far as honesty allows: it does **not** pick a sharpness (that is tuning, forbidden), it
**characterises** the obstacle — naming the single irreducible number and proving everything else is forced.

## What it delivers (all in this folder)
- `run.py` — the deterministic module (self-hashing, 2×sha256). Imports the **frozen** `vp_substrate` cubic
  + `vp_sound_wave` place map, and reuses E1's inverse-Greenwood; **edits no inherited byte** and triggers
  **no re-freeze** (E6 fetches no gene).
- `gate.py` — the standalone gate (G1–G7).
- `FINDINGS.md` — results + seven honest negatives.
- `START_HERE.md` — this card.

## The result in one paragraph
Model the partition near its place as a driven damped resonator whose natural frequency is the **frozen**
Greenwood CF(x); the BM-velocity response is the universal single-pole resonance. From that one form, with
no free choice but the sharpness Q: **(1)** the velocity **peak sits at ω0=CF(x) for every Q** — i.e. exactly
E1's forced place (|Δ|=0.0000); **(2)** the partition reactance `χ=1−(f/CF)²` flips sign at CF, so a tone
**propagates basal** and is **cut off apical** of its place — the asymmetry SIGN is forced and κ-robust;
**(3)** the −3 dB velocity bandwidth is **exactly ω0/Q** (machine precision); **(4)** the active amplifier is
**negative damping** `Q_eff=Q0/(1−G)`, landing at criticality on the inherited **E3 cube root**; **(5)** the
group delay **peaks at CF** with magnitude 2Q/ω0. The whole envelope is therefore a **one-parameter family in
Q**: every LOCATION is forced and Q-invariant, every MAGNITUDE scales with Q and is fixed by **no inherited
constant** — so Q is the **single irreducible [O]**, and a closed numeric envelope would require **tuning** it.

## How to run / verify
```
python3 research/E6-traveling-wave-envelope/run.py     # prints the emergence; ends with 'sha256: …'
python3 research/E6-traveling-wave-envelope/gate.py    # 'E6 GATE: PASS' iff all 7 hold
python3 tools/verify_seed.py                           # whole-seed gate; E6 is in the foundation list
```

## Firewall (binding)
γ reads promoter **structure only** — never a partition damping, a quality factor Q, an active force, a
gain, a delay, or an effect (the amplifier gene is reproduced offline ONLY to anchor the E3 bridge and to
show no inherited byte moved). **No disease claim** — E6 is macromechanics; it neighbours the E4 deafness
goal but states none. No molecule, dose, or efficacy; nothing diagnosed or treated. Every magnitude is
**[O]** with its obstacle named, and the felt percept of pitch/timbre is the **mind** volume's.
