# START HERE — increment E3 (cochlear active amplifier)

The third emergence (BLUEPRINT.md slot E3; delivered in v0.4.0). See `FINDINGS.md` for the result.

**Task (delivered):** the outer-hair-cell **cochlear amplifier** powered by prestin (`SLC26A5`, γ
measured, in the frozen atlas) as the **inherited R19 cubic at its critical point** — yielding the
parameter-free **cube-root compression** `r ∝ F^(1/3)`. The substrate to extend is
`inherited/vp_substrate.py` (the cubic `sdot`/`settle`/`spinodal`), the place map
`inherited/vp_sound_wave.py`, and the measured reading via E1's `read_measured` (γ + A4 from the frozen
cache, asserted == atlas).

**Deliverable (met):** a deterministic module `research/E3-cochlear-amplifier/run.py` that
(a) reads `SLC26A5`'s measured γ+A4, (b) proves the cube-root exponent is the inherited cubic's critical
fixed point (analytic [F]) and verifies it on the inherited integrator ([V]), (c) shows the amplifier and
the E1 switch are one cubic in two regimes, (d) places `SLC26A5` in the spinodal order, (e) self-hashes
(2× → identical sha256), and (f) grades [F]/[V]/[L]/[O] honestly. Plus a small gate (`gate.py`, 7 checks),
folded into the verifier's foundation list. The inherited foundation stayed **frozen**.

**Firewall:** structure-only; γ is never a motor force/gain/dose; the felt percept of loudness is the
mind volume's; the disease layer (E4) is proposal-only.

**Next (E4):** the congenital-deafness genes (`GJB2/GJB6`, `OTOF`, `USH2A/MYO7A`, + `SLC26A4` to fetch)
as R19/transduction/amplifier **failure modes** — direction-only under the firewall — and the named
open target, the full fluid-loaded dispersive traveling-wave **ENVELOPE** (needs Q + fluid loading +
this amplifier; a closed envelope would require tuning Q — forbidden).
