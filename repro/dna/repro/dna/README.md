# repro/dna — 4D DNA Blueprint (v13)

Each chapter slug has a folder here (claim-strip link target). The measured backbone is in `_verify/`
(self-contained, offline): `cd _verify && python3 run_regression.py` → REGRESSION PASS (28 canonical γ cases,
determinism seed=7). DOI snapshot: 10.5281/zenodo.20471407.

Reproduce scope is stated per slug in each folder's README — the γ census and the ZRS γ reproduce exactly;
OTX-family decimals and the ENVIRONMENT cases are principle-demonstration / literature.
