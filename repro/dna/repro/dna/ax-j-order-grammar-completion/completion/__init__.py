# -*- coding: utf-8 -*-
"""
Appendix J -- the order-grammar COMPLETION.

Closes the four open channels Appendix I disclosed, with REAL DATA and no tuning:
  O1  fill the gene set (limb FGF/Wnt inducers + full HOX collinear chains) so the named
      Appendix-I dilutors are removed and the pre-registered 0.70 absolute-strength floor is
      RE-TESTED globally.
  O2  attach real measured rates (segmentation-clock period, Carnegie stage days) to convert
      ORDER -> approximate DAYS.
  O3  read per-edge drive off the promoter sequence (gamma ON-branch amplitude sqrt(gamma))
      instead of a uniform weight W=1.
  GATE generalise the OR-gate wavefront to a QUORUM / AND threshold-k gate.

ADD-ONLY over Appendices A-I: every prior page, number, grade, equation and DOI is unchanged.
"""
from . import (seqtools, lock, cascade, coupled, timing, nulltest, grammar,
               declaration, grading, interpreter, gate)  # noqa: F401,E402

__all__ = ["seqtools", "lock", "cascade", "coupled", "timing", "nulltest",
           "grammar", "declaration", "grading", "interpreter", "gate"]
