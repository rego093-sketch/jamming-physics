# CHANGELOG — v1.14 · Appendices H (the build) and I (the developmental order grammar)

**paper_id** `dna` · **DOI (concept)** 10.5281/zenodo.20471407 · Young Jae Lee · ORCID 0009-0002-7535-8245

This release is **strictly add-only**. Chapters §0–§13 and Appendices A–F — every prior page, number,
grade, equation, and DOI — are **unchanged**. Two appendices are added to the multi-page site, each with
its own fail-closed reproduction package, and the section count rises from 22 to **24**.

The only edit to a prior page is **navigational**: Appendix F's prev/next bar gains a forward link to
Appendix H. No prior body text, claim, or number was altered (the gate's paragraph-completeness check is
unaffected).

## What was added

### Appendix H — Building the body: the constructive grammar
`docs/dna/ax-h-build-the-body/` · `repro/dna/ax-h-build-the-body/`

Appendix F declared the **reading** grammar structurally complete. Appendix H opens the orthogonal
**constructive** axis: it turns the read blueprint into a **build-specified body**. The body is enumerated
as **286 named modules** (206 bones DERIVED from program group counts, not asserted), each grammar-addressed
(program → real GRCh38 driver-γ → emergence order → body system → renormalization-tower level → assembly
parent). Four constructive grammars — the **count** clock G7 (count null |corr(γ, serial)| = 0.000; inverse
lever ×2.00/×0.50 machine-exact), the **size** switch (base·dosage^¾, monotone/rank-preserving), the
**assembly** tree (one connected rooted tree, max depth 7), and the **4D schedule** (argsort(spinodal(γ)),
resorts on γ) — are realized by the **renormalization compiler** (강성화: ρ'=φρ, exact [Reuss, Voigt]
bracket, c'=c√J; 286 modules compiled, body LEVEL 0.7711, R composes as a semigroup, discrepancy 2.6e-13).
Three-axis declaration: structural reading complete + constructive build complete (gate-backed); **physical
instantiation in real tissue OPEN**; no body claimed.

**Gate:** `python3 -m build.gate` PASS 12/12 (`sha=e9dd6d1e15149807`); reading hash `fa919cad71520d2d`;
`structural_complete=True`, `constructive_complete=True`, `physical_complete=False`.

### Appendix I — The developmental ORDER grammar: depth, not stiffness
`docs/dna/ax-i-developmental-order-grammar/` · `repro/dna/ax-i-developmental-order-grammar/`

Closes the open obstacle inherited from Appendix A (single-locus stiffness γ orthogonal to developmental
timing) and from Appendix H's 4D schedule (whether the γ-order matches real Carnegie staging). γ fails to
stage development because it is the wrong **category**: γ is a **local** single-locus key (barrier γ²/4,
spinodal); developmental order is **global** — a gene fires only after its upstream regulators deliver
drive. The predictor of order is **regulatory-cascade hierarchy DEPTH**, which is not a new grammar but the
**G3 architecture/wiring grammar read on the time axis**, composited with the local barrier γ²/4 as a
within-tier tie-break.

Four locks (all `[V]`): **the null** (Spearman(spinodal γ, Carnegie) = −0.27, inside ±0.35); **edge
concordance** (25 cited regulator→target edges among anchored genes — 24 forward + 1 tie, 0 inversions →
concordance 1.000); **depth-beats-γ** (corr(depth, Carnegie) = +0.47 vs spinodal −0.27 on the same genes);
the **OR-gate wavefront theorem** in the coupled-R19 net (every node started on its stable OFF branch
s = −√γ; a non-source fires after its earliest ON regulator — 0 violations; firing-vs-depth +0.79,
firing-vs-spinodal −0.005). **Live handle:** cut SOX9 ⊣ RUNX2 → RUNX2 fires earlier (59.0 → 38.3). Sharpest
on the wired axial trunk FOXA2 → DLL3 → MEOX1 → PAX1 → SOX9 → RUNX2 → SP7 (ρ = 1.000, canalization).

**Honest negative (disclosed, not buried):** the pre-registered floor of 0.70 on the *absolute global rank
strength* is NOT met globally (0.475) and is met only on the axial trunk — named dilutors are limb-bud
TBX4/TBX5 (artificial sources, their inducers outside the 38-gene kit) and a HOX axis represented by its
terminal group only. **Grades:** order DIRECTION `[V]`; absolute rank STRENGTH `[L]` (strongest on the
trunk); absolute TIMING in days `[O]`; per-gene cis-code → drive `[O]`; a built/simulated human `[O]`.

**Gate:** `python3 -m order.gate` PASS 12/12 (`sha=ca6f907e6a2dd932`); reading hash (2×SHA-256)
`f90dea7a08729a3b6fca4689bd1c7c354c71cb688a0184cee188bfa560912e76`; grades V=8 L=2 O=3;
`physical_complete=False`.

## Inheritance discipline (re-locked, byte-identical)

Both appendices inherit the substrate and parameters without retuning. The 38 driver-γ values in Appendix I
are inherited **byte-identical** from Appendix H; the four skeletal/limb driver genes shared with
Appendices G/H/I (**SOX9 1.459260, RUNX2 1.241556, PAX1 1.504372, GLI3 1.298352**) are re-locked
byte-for-byte and gate-checked; the `(LEVEL, SHAPE) = (γ, A4)` operator and `spinodal(γ) = 2·(γ/3)^1.5` are
identical across appendices. Appendix I's README carries the binding **§INHERIT** ("learn the inheritance
discipline first") section so every new session internalizes the inherited invariants, the
derivation-identity / core-subset invariance rule, the sub-model regression suite, and the source-verified
citation rule before adding anything. `inline_magic_numbers: 0` in both packages.

## Note on Appendix G

Appendix G (skeletal/limb) is a sibling volume of the broader program and is **not bundled in this site
export**; it is referenced by Appendices H and I only as the source of the four byte-identical driver-γ
values. Its absence here is why the site's appendix lettering reads A–F, H, I.

## Gate

`python3 gate_multipage.py` → **PASS 274, FAIL 0** (1 expected WARN: source monolith not present for the
optional diff). Section count 24 = manifest rows; every section linked from the hub; sitemap 25 URLs
(hub + 24 sections); no `.tex`/`txt`/`__pycache__` sources in the package.

---

*LOCK → Derive → Gate. No fitted parameters. precision (정밀) ≠ accuracy (정확). 반증 = 발견. 집.*
*Add-only: Chapters §0–§13 and Appendices A–F are unchanged; the 4 shared driver-γ are re-locked byte-for-byte.*
