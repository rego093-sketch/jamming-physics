# cg_inheritance/ — Continental-Genesis verification bundle (inherited, re-verified)

Module 45 inherits three present-tense, chronology-free results from the separate
`vp_continental_genesis` volume. They are re-verified here, not taken on faith (the same
posture the cascade takes toward its engine bundle).

- `repro/assembly_causal_chain_screen.py`  — CG-30: opening forces an antipodal closing (conservation, rate-free).
- `repro/mantle_jamming_state_screen.py`    — CG-26: the mantle is a jammed solid near unjamming (Maxwell medium).
- `repro/closed_loop_dissolution_screen.py` — CG-36: forced convection; the rift is one limb, the convergence its antipode.

Each prints its ledger, recomputes a double-SHA-256, and asserts `REPRO GATE: PASS`.
`CONTINENTAL_GENESIS_IMPORT.md` is the decision note (what is imported, why, and — the
important half — what is NOT imported).

Run: `python3 repro/<name>.py` from any directory. SEED=19.
