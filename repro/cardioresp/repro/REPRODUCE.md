# REPRODUCE

```
python repro/run_all.py                       # full harness: emerge, circulate, stress + oncology,
                                              # disease battery, then the reproducibility suite.
                                              # -> OVERALL: PASS (11/11 checks), drift 0
python repro/_verify/repro_manifest.py        # verify every shipped file against manifest/SHA256SUMS.txt
python repro/_verify/repro_manifest.py freeze # re-pin the manifest after an intentional edit
python repro/_verify/claim_scanner.py         # fail-closed forbidden-claim scan over docs/
python repro/_verify/doc_integrity.py         # grade-legality + [O]/ledger + DOI consistency
python repro/_verify/gates.py                 # research gate + writing-lock status
python repro/_engine/vp_car_engine.py         # just the emergence JSON (+ sha256)
```

Determinism (VP-SPEC C1): BLAS pinned single-thread before numpy; fixed seed; round-before-hash; sorted
JSON keys; the build is wall-clock-free (RELEASE_DATE pinned). Two engine runs yield an identical
sha256; the whole result blob is byte-identical across recomputation (drift 0); the package reproduces
with the network disabled. Numbers shown anywhere are regenerated by the engine; no hand-entered values.
stdlib + numpy only.

Reproducibility suite (R1–R11) inherited from analgesic_threshold_logic v2.0 (DOI 10.5281/zenodo.20733420);
see README.md for the per-gate table.
