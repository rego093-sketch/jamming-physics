# CHANGELOG — cardioresp_vp_site

All notable changes to this package. Grades use the honest vocabulary [F] forced · [V] verified ·
[L] literature-anchored · [O] open. The **scientific engine and its results** (organ emergence,
discriminants, oncology kernel, disease battery) are **byte-identical** across the v0.4.0 → v0.5.0 step
(engine sha `25900a90079a…`). On top of that fixed engine, v0.5.0 inherits and activates the
reproducibility technology from `analgesic_threshold_logic` v2.0, binds the concept DOI, and — in the
writing phase — expands the canonical site to full VP-SPEC v1.8 retrieval-ready depth.

---

## v0.5.0 — 2026-06-18 — Reproducibility-technology inheritance + concept DOI

**Theme.** Inherit the delivery-side reproducibility apparatus matured by `analgesic_threshold_logic`
v2.0 (concept DOI `10.5281/zenodo.20733420`), apply it in full to this package, bind the package's own
concept DOI `10.5281/zenodo.20755371` site-wide, and record the retroactive-application plan for the
sibling packages. **No scientific result changed**; the engine output is byte-identical to v0.4.0.

### Added — reproducibility technology (inherited)
- **File-level fingerprint manifest** `manifest/SHA256SUMS.txt` (`repro/_verify/repro_manifest.py`):
  SHA256 over the whole canonical tree (code + site + inherited assets + governance docs),
  `sha256sum -c` compatible, with `freeze()` / `verify()`. **Fail-closed** — an absent or empty
  manifest is a FAIL.
- **Fail-closed forbidden-claim scanner** `repro/_verify/claim_scanner.py`: 16 curated regex patterns
  over `docs/**`, blocking drug-dosing / prescriptive / treatment-of-disease / efficacy-safety language
  while preserving the legitimate epidemiological vocabulary of the oncology chapter.
- **Document-integrity gates** `repro/_verify/doc_integrity.py`: grade-token legality ({F,V,L,O} only),
  [O] ↔ ledger cross-check, and DOI consistency.
- **Whole-harness determinism + offline gates** added to `repro/_verify/gates.py`: `drift_ok()`
  (recompute the full engine ⊕ stress ⊕ oncology ⊕ disease blob twice, byte-identical) and
  `offline_ok()` (reproduce with the network socket disabled).
- **Unified one-command harness section [7]** in `repro/run_all.py`: eleven gates R1–R11 ending in a
  single `OVERALL: PASS (N/N checks)  drift 0  DOI …` line; non-zero exit on any failure; writes a
  deterministic `reports/repro_suite.gate.json`.
- **`CONSTITUTION.md`** at package root: articles A1 firewall · A2 honest grading · A3 no-tuning ·
  A4 research-first · A5 determinism · A6 reproducibility technology · A7 provenance · C0 single
  deliverable + English body.
- **`zenodo/`** deposit set: LaTeX → PDF whitepaper (scalable fonts, no Type-3) and `ZENODO_METADATA.md`
  (directory excluded from the file manifest, as LaTeX may embed timestamps).
- **`README.md`** rewritten with the one-command reproduce flow and the full R1–R11 gate table.
- **`repro/REPRODUCE.md`** rewritten to list every verification command and attribute the apparatus to
  `analgesic_threshold_logic` v2.0.
- **`FUTURE_WORK.md` §8**: the inheritance record plus the retroactive-application plan and per-package
  inheritance checklist for the sibling packages.

### Changed — provenance binding (no prose change to the science)
- Concept DOI `10.5281/zenodo.20755371` wired into every section page, the hub, the ScholarlyArticle /
  CreativeWorkSeries JSON-LD (`identifier` + `sameAs`), `docs/cardioresp_vp_site/_meta.json`, and
  `docs/llms.txt`; `.doi` claim-strip pill added to the site CSS.
- `tools/build_docs.py` made **wall-clock free**: `RELEASE_DATE` pinned to `2026-06-18` and
  `BUILD_DATE` bound to it (was `datetime.date.today()`), so regenerating the site reproduces it
  byte-for-byte. The previous "pending archival" DOI placeholders are removed.
- Canonical site DOI-wiring step (the provenance binding above) changed only DOI / JSON-LD / footer /
  CSS-pill / `llms` / `_meta` — no prose change at that step; the prose expansion below is a separate
  writing-phase change.
- `VERSION` → `0.5.0`.

### Added — canonical site expanded to retrieval-ready depth (writing phase, VP-SPEC v1.8)
- **Three new sections**, all regenerated by the deterministic builder from the same engine values:
  §10 *From DNA: Master Genes, Stacking Stability, and the R19 Switch* (the explicit gene → γ → R19
  switch → oscillator emergence chain), §11 *Shared-Substrate Cardiorespiratory Spectrum* (Mayer waves,
  airway-tone switch, the obstructive-apnea loop-gain regime, the periodic-breathing spectrum, fever
  HR–RR co-scaling, and excitable cough, stated as mechanism — firewall-clean under R7), and §12
  *Methods, Determinism, and the Reproducibility Apparatus*. Site is now **14 HTML pages** (12 sections
  + hub + top index).
- **DNA-emergence framing foregrounded** in §1 and the hub, with a direct cross-link to §10; minimizing
  phrasing removed throughout (the volume *derives* the functional dynamics on a DNA-anchored oscillator).
- **Search/SEO surface strengthened** per VP-SPEC v1.8 C4: per-page `keywords`/`author`/`robots` meta,
  Open Graph + Twitter cards on every page type, Highwire Scholar `citation_*` tags on the hub, and an
  entity-rich `knowsAbout` in the ScholarlyArticle JSON-LD. Answer-first blocks, self-contained
  `vp-card`s, and `≤3`-sentence paragraphs retained.
- **Ledger** gains a third `[O]` row for the §11 shared-substrate absolute magnitudes (same calibration
  obstacle as absolute organ size and absolute cancer relative-risk); R9 cross-check covers
  open sections 1, 8, 9, 11.
- Engine output unchanged by the expansion (sha `25900a90079a…`); every displayed number remains a
  regenerated engine value, and R7/R8/R9/R10 stay green.

### Verification at release
- `python repro/run_all.py` → **OVERALL: PASS (11/11 checks), drift 0**, DOI `10.5281/zenodo.20755371`.
- Engine determinism R1 (sha `25900a90079a…`), whole-blob drift-0 R2, stress T1–T5 (5/5), oncology
  forced shape R4 [RR(0)=1, no threshold, barrier match], disease battery 6/6, claim scan clean
  (16 patterns / 14 pages, 0 hits), grade legality, [O]↔ledger (open sections 1, 8, 9, 11), DOI
  consistency, offline self-containment.

---

## v0.4.0 — 2026-06-18 — Disease-extension battery + writing phase (baseline)

- Research signed off and `PHASE` advanced to `writing`; canonical multi-page site emitted under
  `docs/` (VP-SPEC v1.8): 9 published sections — cardiorespiratory overview, shared FHN oscillator,
  eupnea, apnea/chemoreflex loop-gain, RSA, Cheyne–Stokes, baroreflex, carcinogen dose–response,
  reproducibility-grades/open-items.
- T1–T5 discriminant stress battery; carcinogen dose–response kernel sharing the R19 bistable switch
  (RR(0)=1, no threshold, convex shape).
- Disease-extension battery 6/6 (Mayer waves, asthma, OSA, central-apnea spectrum, fever HR–RR
  co-scaling, cough), as a research artifact reusing T1–T5 + the oncology kernel with no new primitive.
- `IRREPRODUCIBILITY_LEDGER.md` with the two [O] items (absolute organ size; absolute cancer RR).

*(Pre-v0.4.0 history — substrate vendoring, organ emergence from measured γ, the shared-oscillator
derivation, and the T1–T5 discriminant design — is summarised in `START_HERE.md` and `CHARTER.md`.)*
