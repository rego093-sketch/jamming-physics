# START HERE — increment E2

The second emergence of the nose seed (see `BLUEPRINT.md` for the full plan). **BUILT (v0.4.0).**

**Task:** the **olfactory transduction switch** — where the sibling skeleton SURVIVES. The olfactory
cascade (odorant → OR → Golf/GNAL → ADCY3/cAMP → CNG channel CNGA2·CNGA4·CNGB1 → ANO2) ends in the
**same all-or-none R19 flip** as vision. Use the measured γ already in `inherited/organ_gamma.json`
for the six transduction genes and the R19 switch in `inherited/vp_substrate.py`.

**Deliverable (done):** a deterministic module `research/E2-transduction-switch/run.py` that
(A) settles the FROZEN R19 field from rest under a drive sweeping each gene's spinodal and shows the
**discontinuous all-or-none flip** (dark below h*, snaps on above, finite jump ≈+2.2); (B) shows the
**cubic −s³** is the cooperativity (order n=3) and is **necessary** — deleting it gives a graded
response (≈229× shallower across the fold); (C) the **cross-sense** result: **CNGB1** (CNG β) is the
*same gene* in rod vision and olfaction, its measured γ byte-identical to the rod-vision sibling — the
transduction switch is **shared, not analogous** — and the CNG subunits read distinctly by (γ, A4).
It prints every number and self-hashes (2× run → identical sha256) and declares grades [F]/[V]/[L]/[O]
honestly. Plus `gate_E2.py` (`E2 GATE: PASS`), folded into the verifier's foundation list. The
inherited foundation stays **frozen**.

**Firewall:** structure-only; the order-parameter `s` is the abstract R19 field, **not** a receptor
current/voltage/Hz; no dose/efficacy; the `_linear_control` is the substrate with its cubic struck out
(it proves necessity, it does not fork the substrate); the felt percept of smelling is the mind
volume's. The absolute odorant→drive→firing (Hz) scale is a named [O].

**Next:** `BLUEPRINT.md` → **E3 — the bulb map** (glomerular convergence turning the receptor-space
code into a spatial odour map; targeting specificity is [O]), then **E4 — congenital ANOSMIA** (the
goal): CNGA2/CNGB1/ANOS1/FGFR1 (loaded) + PROKR2/PROK2 (to fetch) as R19/transduction failure modes,
direction-only / proposal-only under `FIREWALL.md`.
