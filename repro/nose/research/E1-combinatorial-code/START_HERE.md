# START HERE — increment E1

The first emergence of the nose seed (see `BLUEPRINT.md` for the full plan). **BUILT (v0.4.0).**

**Task:** the **combinatorial code** — the honest departure from the sibling senses' skeleton. Vision
and hearing are *wave → single physical place → R19 switch*; smell has **no wave** (the stimulus is a
molecule), so identity is a **combinatorial pattern** over a large olfactory-receptor (OR) repertoire
(Buck & Axel). Use the **measured reading** already in `inherited/organ_gamma.json` (each OR gene's
γ LEVEL + A4 SHAPE) and the R19 switch in `inherited/vp_substrate.py`.

**Deliverable (done):** a deterministic module `research/E1-combinatorial-code/run.py` that
(A) treats each OR as an inherited R19 switch and orders the bank by **spinodal(γ)** (the *only* thing
γ supplies here — excitability, not ligand match); (B) shows the code is **combinatorial** (capacity
2^N ≫ N), with a uniform drive giving a substrate-derived **thermometer** readout (≤N+1 nested
patterns) while the full richness needs an odorant-specific **drive vector** = the binding-pocket
**[O]**; (C) states the **honest negative** — smell's "what" (odour) is **NOT** a substrate quantity
(unlike vision's colour = angle χ); the OR (γ, A4) readings are distinct but encode expression
structure, never odorant tuning. It prints every number and self-hashes (2× run → identical sha256)
and declares grades [F]/[V]/[L]/[O] honestly. Plus `gate_E1.py` (`E1 GATE: PASS`), folded into the
verifier's foundation list. The inherited foundation stays **frozen**.

**Firewall:** structure-only; γ is promoter structure (threshold/expression), **never** an odorant
affinity, occupancy, rate, or percept; the odorant→receptor drive is a named [O] (binding pocket);
the abstract drive vectors only illustrate the readout (not measured affinities); the felt percept of
smelling is the mind volume's.

**Next:** `BLUEPRINT.md` → **E2 — the transduction switch** (the surviving R19 skeleton: the
olfactory CNG channel as the all-or-none flip, shared with rod vision via CNGB1).
